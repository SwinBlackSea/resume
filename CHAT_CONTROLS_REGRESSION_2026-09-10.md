# 聊天操作精简、单次上下文与双向导航 · 2026-09-10

在 `feature/resume-first-flow` 保留已有成果继续修改，不改历史原型、不删除存量业务数据。

## 已实现的范围

- 原续聊提示改为原生下拉框“延续当前对话 / 不延续当前对话”。后者只影响下一条提交；切换本身不发消息、不删历史、不改简历。
- 服务端受理 `context_mode: fresh` 后创建同一对话中的独立任务。旧任务的文字、记忆、建议、原图、识别文件及链接不进入新任务；新任务仍可读取当前草稿、已保存可选资料和已确认岗位。本轮原始用户要求完整透传，不加入需求关键词规则。
- 文件/链接此前按 conversation 汇集，现与文字和图片一致按 task 隔离，后续轮次和重试仍复用一次识别结果。
- 受理前失败保留 fresh；受理后（包括模型失败）恢复 continue，重试使用失败消息身份复用同一任务，不重复消息或再次创建任务。标签页刷新按 client_request_id 判断单次选项是否已被消费。
- 修复已完成生成的 HTTP 响应丢失后，前端可能保留旧任务身份的问题：先同步运行状态，再恢复服务端当前任务，下一轮不能误接旧任务。
- 图片解码后重新核对提交身份：同一 client_request_id 的并发请求即使一条已生成完成，也不会再次创建任务或调用模型。
- 全局简历建议卡片移除“暂不使用”。保留旧建议及已有预览、应用、继续调整能力；不移除局部改写、岗位或其他独立业务动作的取消能力。
- 删除聊天头部固定“当前目标岗位”和“上传文件 / 补充经历 / 提供或更换岗位”三项重复入口。输入框附件和自然对话仍可补充材料/更换岗位，岗位变化仍需确认。
- 新对话、收起/关闭按钮普通态和悬停态透明，保留点击面积与键盘焦点指示。
- 详情页 resume 先保存再回首页；首页 resume 返回当前标签页刚才编辑的简历，不误选后来新建的另一份。记忆项目须在有权限项目列表中校验；无可返回简历时禁用，不创建/覆盖文档。首页尚未发送内容沿用现有保存链。
- 追加的完整简历差异审阅稿单独委派实现并统一验收，见 `RESUME_REVIEW_REGRESSION_2026-09-10.md`。

## 专项验证

真实 Chromium 和隔离数据库覆盖：

- 下拉框键盘切换、320/390/1440px 可用性、被动刷新、重新加载、生成中锁定。
- 生成中刷新后恢复等待；成功/失败后的单次选项消费；受理前错误保留输入和选择。
- 原图、文件、链接、建议、历史及记忆隔离；新材料在继续和重试中保留。
- 非法 mode、fresh 与旧任务/建议/快捷回复身份冲突、重复提交、不变的旧任务及旧建议。
- 服务器完成但响应丢失时查询恢复，不重发生成，后续使用正确任务。
- 控制图片解码时序模拟并发提交，先到请求暂停、后到请求完成后再恢复，确认仅一条用户消息、一项任务、一次模型调用。
- 导航离开前保存、返回同一份简历、不修改另一份草稿、首页输入保留、无效项目记忆及无简历入口。

控件专项 12/12 通过；新增并发保护后上下文专项 4/4 通过。前序组合测试中的新差异界面改变了旧片段比较范围，已将断言迁移为“整份可读简历存在，同时定位变更区域逐行核对”，没有删除差异覆盖。

日志：

```text
/tmp/resume-controls-verified-20260910.log
/tmp/resume-context-concurrency-20260910.log
/tmp/resume-review-image-final-20260910.log
/tmp/resume-chat-review-verified-20260910.log
```

本轮不新增真实 Astra 调用，不以注入模型的通过率代替供应商准确率或延迟结论。运行时模型、生成提示词与事实规则未改；`SYSTEM_PROMPT.md` 仅同步输入隔离契约说明。

## 发布检查

稳定代码全量执行：

```sh
RESUME_NODE_SAMPLE_DIR=/tmp/resume-node-samples-9NFWHN npm test
```

405 项中 399 通过、0 失败、6 跳过，耗时约 324 秒。6 项为显式启用的真实模型调用测试，本轮未启用。包含 14 种布局、1008 次真实浏览器增删/撤销/重做操作，以及公开 Word/PDF 简历样本回归。

已在确认无运行中任务后重启 PM2 `resume`；入口与新增 `resume-review.js` 均 HTTP 200。重启前后核对 9 张业务表内容一致，未清空简历、资料、附件或历史版本。

生产真实 Chromium 只读复查通过：默认及切换下拉框、透明悬停按钮、冗余入口移除、附件入口保留、当前简历预览/导出入口、编辑页 → 首页 → 同一编辑页。浏览器异常 0；复查前后 10 张表（含撤销记录）内容一致。

生产当前会话没有保留可预览的旧建议，首次只读检查因此停止在该检查点；没有为补测而调用模型、创建建议或改动用户正文。建议差异、首次/再次应用和实际 PDF/Word 下载由全量中的隔离真实浏览器测试覆盖，线上只读复查改为当前正文预览后通过。日志：`/tmp/resume-controls-production-readonly-20260910.log`。

“生成过程中追加消息/即时打断重启”不在这次已确认下拉框的实现范围内，尚未开放。
