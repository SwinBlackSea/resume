# 人工增删布局回归 · 2026-09-09

## 问题与修复

用户报告“增加模块内容失败、子节点增加无效果”。只读检查当前草稿发现：新增事务已保存，但副本保留了原节点的 `grid-row/grid-column`，多个内容占据同一个格子。节点数增加不代表用户看到了新增内容。

本次不调用模型，也不按“关键成果”等模块名称匹配：

- 复制完整子树与展示样式，另行分配 Grid 占位；后续显式行位置一起调整，跨行节点按闭合行带处理。
- 覆盖数字、长写/短写、负索引、命名行、命名区域、跨度及嵌套结构；自动网格继续使用浏览器自动布局。
- `display:contents` 按实际布局父容器处理；Flex 多节点子树整体复制不再与原内容交错排序。
- 删除只收拢已经没有其他内容占用的行。位置调整、增删、自动保存共用一个可撤销事务。
- 旧版已保存的同格、同内容副本，在该网格再次新增时分配独立行，保留全部内容；不自动删除副本，也不整理内容不同的叠层。

## 真实浏览器结果

真实 Chromium、真实 `index.html`、HTTP 接口与 SQLite 草稿事务。模型未参与。生产文档只读加载到独立测试数据库，未在生产草稿上点击或应用修改。

| 范围 | 覆盖与结果 |
|---|---|
| 14 个可复现布局变体 | 普通文档流、Flex、Grid、contents、数字/长写/负索引/命名行/区域/跨行、嵌套子树、旧重叠副本 |
| 用户报告的实际文档 | 从真实能点击的最上层旧副本开始，而非绕过遮挡调用内部函数 |
| 每个布局 | 100% / 125% / 150%；增加同级内容、增加模块内容、新增同级模块；各连续新增两次 |
| 每次新增 | Chromium 文字范围实际坐标检查：新内容可见且不覆盖原内容；检查子树、节点身份、撤销、重做 |
| 删除与持久化 | 真实删除、撤销、刷新后重新读取同一草稿；脚本异常检查 |
| 操作总数 | 15 种布局 × 72 次 = **1080 次**，全部通过 |
| 公开文件另测 | Harvard 两份 Word、Harvard PDF、MIT 14 页 PDF；含两份固定布局投影，共 6 个视图，**99 次**结构操作通过 |

公开文件使用已下载且 SHA-256 固定的字节，本轮没有重新请求模型识别。固定坐标视图沿用原有能力边界，不把禁用新增算作成功新增。

## 复跑

```bash
# 合成布局矩阵；不读取生产文档
node --test tests/manual-flow-layout-browser.test.js

# 显式只读复现本次生产文档；写入的仍是隔离测试数据库
RESUME_MANUAL_PRODUCTION_REPRO=1 node --test tests/manual-flow-layout-browser.test.js

# 已下载的公开样本，通过真实解析和浏览器测试
RESUME_NODE_SAMPLE_DIR=/tmp/resume-node-samples-9NFWHN \
  node --test tests/manual-structure-browser.test.js
```

本次运行日志：

```text
/tmp/resume-manual-layout-browser-final.log
/tmp/resume-manual-public-regression.log
/tmp/resume-release-grid-fix.log
```

## 边界

这是明确布局类型和样本的回归覆盖，不代表穷尽任意 CSS 或所有简历。保留原有固定坐标/页面底图限制。本次不重写 AI 提示词或事实规则，不清理生产简历内容，不修改历史原型。

## 发布检查

- 全项目回归：365 项，360 通过、5 项条件跳过、0 失败。
- 服务已重启，HTTP 200；生产页面 Chromium 只读复查无脚本异常。
- 重启前后十张受保护业务表内容摘要一致；保留原简历、资料、岗位、附件与历史。
- 全局 Astra、局部 DeepSeek Flash 配置不变；本次增删修复没有调用模型。
