# AGENTS.md

## 核心准则

正常时一溜到底，异常时不拉稀摆带（快速抛错止损），失控时把主动权交还给用户。

## 项目约束

- `index.html` 是真实代码入口，产品交互以实际运行结果为准。`index.prototype.backup.html` 仅为历史参考，不再维护、不作为验收依据，不得另建替代原型 HTML。
- 停止原型一致性验收；仍有价值的行为断言必须迁移为真实实现功能测试，不能因此减少覆盖。
- 面向完全不懂简历工具的用户：使用结果导向的日常语言，避免“来源、证据、分支、快照”等技术词直接占据主界面。
- 产品围绕满意的简历，首页按 2026-09-10 用户确认的新要求展示个人信息、岗位信息、简历模板三个显眼材料区；三项材料可用后才能生成。个人信息支持 Word/PDF/截图，岗位增加岗位链接实时验证，模板增加系统内置版式参考。上传不立即生成，不自动建立个人档案；编辑页继续使用“当前简历＋AI 对话”，不恢复左侧固定资料管理模块。
- “制作另一份简历”复用项目隔离机制，离开前保存当前修改；复制完整文档而非覆盖原稿或建立版本树。每份简历有独立岗位、对话和历史；取消“我的简历”独立界面与首页列表但保留已有数据、项目地址和 resume 返回当前简历能力。更换当前岗位与制作另一份简历必须区分。
- 首页生成完成先在首页预览，可重新提供材料、下载 PDF/Word，或点击“继续编辑”进入详情页。新制作不覆盖旧简历；三项材料准备、识别、失败及链接验证状态同时在客户端和服务端校验，迟到请求不得覆盖新材料。个人事实、岗位要求与版式参考必须按角色传递，不把参考简历的示例经历或头像当成求职者信息。
- 首页生成结果在居中弹框预览，PDF、Word 下载直接可见，关闭后保留重新查看入口，不在三材料区下面平铺正文。截图粘贴即时本地预览，图片与文件名可弹框查看原图，上传期间可删除/替换；首页截图安全校验后作为视觉材料就绪，生成时读取原图，不强制先排队 OCR。Word/PDF 保留一次识别链，材料与冻结任务引用共同保护图片生命周期。
- 首页“开始生成”授权从空白制作首份文档，生成成功自动保存草稿和不可变版本。已有文档（含复用稿）的 AI 修改仍须“应用修改”；生成期间用户已经编辑草稿时不得自动覆盖。保存版本不清空有效的五步撤销/重做。
- 当前简历在技术上是唯一的完整可编辑文档，正文、页面结构、样式、资源和可选语义标记均属于该文档；不得再以正文、模板和槽位绑定三个独立对象作为现行模型。
- `ResumeDocument.children` 是唯一持久化父子关系，节点 `semantic.kind` 表达文档、页面、模块、标题、段落、列表和表格等语义；不得双写 `parent_id`。PDF、DOCX、DOC 和图片导入必须归一化为同一语义树。
- 首页“简历模板”是上传或内置的完整文档版式参考，不建立模板绑定、槽位、模板版本树或第二份当前简历。生成后页面、样式与资源都属于唯一 ResumeDocument。导入文件经一次识别，后续复用识别结果，不逐轮重新识别。
- 系统样式提供本地缩略图列表，点击后弹框预览，只有“应用”成功才更换本次材料；取消预览保留原选择。第三材料卡展示已选样式的缩略图和名称；选择不自动生成。模型读取对应的完整参考图，仅参考排版，不得使用示例人物的事实或头像。
- 第三材料卡的已选样式缩略图只查看当前样式的大图；“系统样式”按钮才打开完整图库。桌面全局 AI 区保持原先 390px，不为填充留白而扩宽；释放的空间归画布，简历纸张尺寸和正文不拉伸。
- 人工编辑支持点击正文直接改字，`+/-` 遵循“框住什么，就操作什么”：指向文字选择当前编辑节点，指向父容器边缘选择完整父子树，始终只有一个范围框和一对按钮，不提供层级选择菜单。`+` 完整复制框内子树到其同级下方，`-` 直接删除框内子树并支持撤销。标题不自动升级为整个模块；移动、合并、拆分、样式、页面结构和跨区域联动仍通过 AI 建议完成。
- 任意深度子树及文字样式完整保留，按下按钮时锁定稳定节点 ID，不按按钮坐标重新猜测目标；表格合并行采用可见的单个闭合行组范围框。结构选择不改变 AI 的 editable 编辑边界，删除末项不额外删除未选中的父容器。
- 鼠标从已选节点移向其 `+/-` 时，节点到按钮之间的通行区域保持当前目标，不因穿过父节点、子节点或兄弟节点而切换范围；途中停顿及保存刷新不得跳层，离开通行区域后恢复正常悬停选择。浏览器测试必须包含连续鼠标路径，不能只直接定位到按钮。
- 不提供“我的简历”独立界面和按钮，点击 resume 标识在首页与当前详情之间导航；“制作另一份简历”放在个人中心。个人中心始终位于顶栏最右侧，包括窄屏。预览/下载位于文档工具栏；工具栏首位、撤销旁的保存图标直接保存到历史版本，历史版本与设置入口放在个人中心。编辑页不再提供重复的“生成简历”，首次生成保留在首页，后续修改通过 AI 对话并等待应用确认。
- 设置提供默认关闭的“显示内容增删按钮”开关，在当前浏览器记忆显式选择；关闭时隐藏并停用 `+/-`，不改变正文、不影响文字直改、AI、自动保存与撤销。开启时悬停即显示，不需要点击或聚焦，正文重渲染后恢复真实悬停状态；按钮必须留在纸张内部。
- 点击正文以外空白时清除临时选中框、文字选区、编辑焦点及工具条，不仅隐藏按钮；待保存文字沿用失焦保存链，保存刷新不得恢复已取消的悬停框，不清空 AI 任务或建议。再次指向/点击正文可正常继续操作。
- 聊天卡片只保留建议摘要和操作，“查看差异”打开居中弹框，按完整行显示 Git 式差异：删除行红底及删除线，新增行绿底，不逐字切碎；未变化行只展示一次，可折叠但不截断修改。整份建议预览与首次/再次应用均使用同一三方合并结果；预览绑定当前草稿 revision，预览后正文变化时须重新预览，不能静默应用与所见不同的结果。
- 差异弹框主体是一份完整、只读、可阅读的简历审阅稿，在内容所在区域整行标出新增和删除，未改内容只出现一次，不以节点 ID 或属性列表作为主界面。结构、样式、页面设置变化在对应区域用日常语言标注，具体前后值可展开，不能将纯排版建议显示为空差异。阅读排版需明确区别于实际版式；实际效果从整份预览查看。整份建议预览内可直接应用或导出 PDF/Word；导出的是该预览对应的合并稿，不是旧正文，也不隐式应用或保存版本。应用与导出都锁定预览 revision，正文变化时须重新预览。
- 全局聊天图片不得自动写入资料；使用私有上传 ID，支持选择、粘贴、拖入及纯图片发送。任务内后续追问继续读取原图，当前或历史含图均使用视觉能力；新对话提交后清理无引用的旧聊天专用图片，局部 AI 保持纯文本轻量协议。
- 简历内的头像和图片使用同一所有者的不可变文档资源 ID，不依赖聊天附件链接或公开 URL。Word/PDF 优先提取原生图片，截图/扫描页使用视觉候选及归一化裁剪坐标；模型声明使用哪个输入图片、裁剪范围和真实目标 img 节点，服务端执行授权、边界检查与确定性资源写入，不允许模型猜测资源地址。草稿、建议、有效撤销、历史和复制稿保护图片生命周期。预览及 PDF/Word 导出必须保留图片，资源无法安全读取时明确失败，不悄悄生成无头像文件。
- 正文内独立图片可点击或键盘选择后上传更换，不调用模型；先保存未提交文字，再按项目、节点 ID 和 revision 锁定提交。保留节点 ID、位置、尺寸与原有 object-fit，不默认裁剪新图；只替换不可变资源引用，复用统一草稿事务及五步撤销/重做，不覆盖旧文件。整页扫描和页面场景背景不得作为独立头像替换。一次性上传身份清理不得删除草稿、撤销或历史仍引用的图片。
- 一个 AI 编辑单元必须对应一个真实的 `editable=true` 内容节点；该节点内部可以保留多个段落和行内格式，但任何后代不得再次标记为 editable。禁止通过范围覆盖属性制造“父节点整体可编辑、子节点又分别可编辑”的双重身份；合并和拆分必须使用明确、可逆的编辑节点语义操作。
- AI 作用范围只显示三种结构：`@资料 · 具体对象`、`@简历 · 具体内容`、`@整份简历`。显示文案不得替代内部稳定类型和对象 ID。
- AI 围绕简历自然聊天：明确的修改要求直接生成可预览建议，包括跨模块、文字、结构和样式联动，不因复杂度强制先确认思路。只分析、解释或讨论时直接回答；用户要求先讨论思路时才讨论；最终结果存在真实歧义时才简短追问。不得询问节点 ID、锚点等内部问题，已确认思路直接执行。已有简历仍以应用修改作为改变正文的确认边界。
- 资料与简历严格分离：资料是可选的基础信息，不是简历的唯一依据；简历可以结合资料和当前对话完善，也可以完全通过用户与 AI 沟通完成。
- 用户在对话中明确提供的内容可以进入简历修改建议，但不得自动写入资料；保存到资料和应用到简历必须是两个独立动作。
- 已有简历的 AI 建议未经“应用修改”不得改变正文；首页授权的首次空白生成除外。资料变化不得静默改写简历，简历变化也不得自动回填资料；岗位变化需由用户确认。
- AI 响应协议只保留 `message` 和 `proposal`：最终结果形成前的回答、追问和处理思路使用自然语言 `message`；只有最终 `proposal` 进入结构校验、差异预览与应用流程。
- AI 上下文按 `owner → project → conversation → task` 隔离。继续调整必须携带任务基线 A、上一版目标 B、最新草稿 C、原始目标和任务内有效对话；模型接口无持久会话时由服务端重建并重发，前端不得承担上下文拼装。
- 全局 AI 使用 `resume-ai-context-v3` 完整可操作文档投影，保留全文、稳定 ID、真实父子关系、行内格式、编辑边界、原生样式、布局坐标、页面设置及资源描述；只省略二进制资源和编辑器临时数据，另提供真实入口中与文档相关的默认 CSS。局部 AI 只发送完整可见正文、可选资料、岗位和锁定文字的纯文本，不发送树或节点 ID。两者共用“系统规则 → 只读上下文 → 当前任务 user/assistant 历史 → 本轮 user”；局部本轮使用 working-set JSON，`instruction` 原样携带要求、`editing_text` 明确唯一编辑对象。投影不得持久化为第二份文档。
- 早期任务对话超过预算时先生成并保存滚动记忆，保留有效要求、确认、否定、用户事实和未决问题；最近对话与本轮要求保留原文。缓存必须校验任务及已覆盖消息前缀；任务使用期间原始历史不删除，摘要失败不得静默截断。重试诊断不是用户消息，最后一条仍为原始本轮 user，不得在恢复时擅自收窄用户要求。
- AI中间数据有明确生命周期：全局新对话创建成功后，在同一事务删除本项目此前对话、任务记忆和建议，迟到结果不得回写；局部应用成功或明确放弃后清空该任务历史，失败和继续调整时保留有效链。只保留无正文的最小幂等/撤销身份回执，当前草稿、资料、版本和有效五步撤销/重做独立保留；无效撤销正文即时清空但保留操作摘要，不能仅按时间删除有效重做。
- 全局聊天下一句话自然承接当前任务；普通解释不作废上一版建议，应用后仍可围绕最新正文继续聊。同一对话一次只生成一轮；刷新可查询无正文状态并恢复等待，停止生成以运行 ID 校验并阻止迟到回写。失败重试由后端按失败消息 ID 重建原请求，不复制用户历史，也不把服务端错误当作 assistant 对话。模型恢复保留独立修改，真实冲突不由后端猜测；最终建议批次事务提交。
- 全局续聊身份由服务端解析；客户端遗漏 task_id 时，同一对话最新任务且作用范围一致则继续，不得仅因取消临时选区而新建任务。`@整份简历` 表示操作范围，不表示重新开始；有未应用建议时以上一版 B 为操作文档，应用后以最新 C 为操作文档并保留对话。显式“继续调整”同时恢复建议的任务与范围；自动保存或被动刷新不得覆盖用户刚选的范围。工作区返回无正文 continuation 描述，前端只展示和引用身份，不自行拼接模型上下文。
- 范围旁提供“延续当前对话 / 不延续当前对话”下拉框。后者仅下一次发送以当前正文开启独立任务，不读取旧任务的对话、记忆、建议、图片、链接和文件；不删除旧记录或改写正文，不等于“开始新对话”。服务端确认已收到该轮后恢复延续；受理前失败保留选择，已受理失败重试复用该轮任务。刷新和重复提交不得再次消耗或重复创建独立任务。全局简历建议卡片不再提供“暂不使用”。
- 聊天头部不常驻目标岗位和三个补充材料快捷入口；附件通过输入框添加，经历和岗位通过自然对话补充，岗位变更仍须确认。新对话及收起按钮无底色。详情页 resume 返回首页并先保存，首页 resume 返回刚才编辑的有权限简历；无可返回简历时不创建或覆盖内容。
- 局部与全局 AI 共用生成态按钮、旋转标识和动态圆点提示，字号、配色与间距一致；生成中禁用但不显示为普通灰色不可用按钮，成功、失败及关闭后恢复。保留状态播报并尊重减少动态效果设置；视觉复用不合并局部与全局的请求、任务和取消逻辑。
- 全局模型输入以editing_document明确本轮唯一操作文档；A/C仅作reference，任务过程字段与未确认思路副本不重复进入系统上下文。输出使用resume_assistant_response_v3结构化修改列表、约束及资料/岗位动作；岗位payload必须包含title/company/confirmed_text，资料payload使用field/value，只有开放节点使用短JSON字符串。混合动作失败时只修复失败动作并原样保留已校验的独立动作，不遗漏、不猜填、不静默丢弃；最终批次统一校验、事务提交。当前部署局部与全局文字、图片及恢复均使用gpt-6-astra（独立OpenAI Responses网关），局部仍为轻量纯文本协议；该网关不支持none，adapter显式配置最低low，全局默认low，输出预算预留推理空间。complex是文档能力标签，型号与推理强度均可配置，不按用户关键词切换。
- 业务 server 负责归属、任务持久化、建议应用与事务；harness 负责上下文、记忆、输出协议和有界恢复；model-gateway 负责统一模型请求、取消和无正文计量，供应商 adapter 仅负责协议转换。局部首轮固定轻量 text，全局固定 complex，图片使用 vision；不得根据关键词决定用户的操作权限。
- 候选模型对照测试与线上配置隔离；OpenAI 使用独立地址和密钥，全局 complex/vision 与基础 text 可独立配置；当前按用户要求均使用 Astra。测试只用虚构数据，不读取 Codex 凭据或写入业务数据库，不更改用户指令、事实规则和应用边界；有效 Key 实测前不得把离线协议通过汇报为模型质量提升。
- 局部 AI 的“继续调整”必须以上一轮尚未应用的 `suggestion` 为直接编辑对象，同时读取最新草稿和完整简历理解语境；前端只传上一动作 ID，上下文归属、上一版内容和轮次由后端校验与组装。
- AI 简历修改默认只返回最小目标片段和紧凑新增声明，服务端继承未返回的展示字段并确定性形成完整目标 B；完整 `target_resume_document` 只用于文档元数据或确实无法用非重叠片段表达的整份重构。服务端以模型读取的草稿 A、目标文档 B 和应用时最新草稿 C 做确定性三方合并得到 D：AI 未修改处保留 C，用户点击应用后 AI 明确修改的字段采用 B；只有目标节点或父节点已不存在、新增 ID 被占用、合并形成非法结构等客观不可执行情况才重新生成。
- 父节点仅返回 `id/style/attributes` 时，未返回的子树仍继承；其与后代的独立字段修改可由 harness 确定性合并为非重叠目标，不依赖模型操作顺序。父节点删除、改写全文或显式替换 children 与后代修改仍属于冲突，不自动取舍。
- 产品和后端均不建立“来源”、证据映射或资料到正文的派生关系。只保留完成撤销、并发控制和安全审计所需的操作记录。
- 每次轻量文字编辑或已应用的 AI 修改都自动保存当前草稿，并统一进入最近 5 步的撤销/重做栈；文字、增加、删除、移动及结构调整必须使用同一事务机制。撤销后产生新修改必须清空重做分支，但不删除审计记录。草稿操作不自动进入历史列表；连续输入应合并为有意义的文档事务，不按单个按键保存。
- 用户可将当前草稿“保存为版本”；一键生成成功必须自动创建不可变历史版本。版本需冻结个人资料、岗位信息和完整简历文档。
- 历史版本使用居中浏览器，支持独立详情、查看当时资料、与当前草稿对照、下载和“复制此版本继续修改”；复制完整文档且不得覆盖原版本或建立版本树。
- 真实实现、PRD、TECH、系统提示词和自动化测试中的关键行为必须保持一致。保留现有资料、接口与数据；移除固定资料界面不等于删除存量数据。
- 当前全局会话中已应用的建议支持“再次应用”，不依赖五步撤销。保留带校验的压缩执行材料，按需读取，不随聊天刷新反复传输；新对话清理时一并删除。再次应用复用三方合并与草稿事务，保留无关手改；旧执行材料已清理时明确告知不可精确恢复，禁止仅凭文字重建旧排版。已应用状态不再用禁用按钮表示。
- 可见正文与编辑标记是不同概念；纯文本提取必须覆盖可见的非 editable 文字。整份重构仍继承省略的样式，模型须显式清除失效布局属性，并为新增文字提供编辑边界。
- 人工增删不调用模型；复制展示样式不等于复用已占用的位置。显式 Grid 新增须分配不重叠的行位置并同步后续占位，跨行按闭合行带处理；Flex 的 display:contents 子树复制不得交错排序。位置调整和节点增删进入同一可逆事务。回归必须以真实浏览器检查新增内容可见、不重叠、完整子树、保存及撤销重做，不能只断言节点数量。
- 账号采用独立登录页、账号密码及开放注册，风格与 resume 一致；登录账号不等于简历个人资料。保留 `users.id` 作为业务 owner，所有业务接口以经过校验的会话确定归属，生产不接受 `x-user-id` 或共享 demo 回退。客户端 owner 标记仅用于阻止旧页在新账号下提交，不构成授权。
- 旧共享资料归保留用户名 `admin`，通过本机显式预检和事务绑定旧 owner，不改写或合并业务正文；`admin/admin` 只能是 HTTP 不可登录的受限初始化态，公开启动前必须本机设置强密码。禁止按首个注册者或邮箱自动认领旧资料，公开注册不得抢占 admin。
- `admin` 是超级管理员，权限由持久化角色与服务端会话校验；可搜索账号、启用/停用普通账号、退出其所有登录，不因此获得其他账号简历内容权限。禁止停用超级管理员或通过公开注册提升权限；管理动作记录操作者和目标，停用/强制退出阻止迟到登录。个人中心使用圆形头像入口，改密与登录设备默认折叠，不展示个人登录时间；管理员列表展示每个账号独立持久化的最近成功登录时间，刷新、改密、失败登录和审计清理不得改写该时间。
- 密码只保存有盐的有界异步 KDF，登录注册有持久限流，会话令牌只保存哈希；Cookie 必须 HttpOnly、生产 Secure，并校验部署路径、Origin 及所有写请求的 CSRF（含原始上传）。改密/撤销后阻止旧凭证迟到登录；私有图片、下载和 API 不可缓存，长连接有界重新认证。账号未配置或初始化未完成时生产启动失败，不恢复演示认证。
- 登录后才加载业务文档，退出前保存未提交文字，退出或切换账号清空旧文档、聊天、附件 URL 和业务缓存并取消请求/SSE；不得把旧账号待保存内容提交给新账号。个人中心仍位于右侧，提供账号设置、改密、会话管理及退出。未配置邮件服务时不伪造在线找回，使用明确的本机运维重置流程。
