# 简历修改审阅回归 — 2026-09-10

## 用户问题与处理

原“查看差异”主要把变化节点的文字或 CSS 属性逐条列出来；大量排版修改时，用户看到的是属性清单，无法把修改与简历正文对应。

现在弹框主体是一份完整的只读简历审阅稿：

- 保留标题、模块、列表及表格的阅读层级；未修改文字只出现一次。
- 修改按完整行展示原文红色删除线、新文绿色；删除内容放在原区域，移动内容只在新位置出现一次。
- 字号、对齐、间距、位置等变化在对应内容附近提示，具体前后值默认折叠，仍可完整查看；主界面不展示节点 ID。
- 采用流式阅读排版，明确不等于最终版式，避免绝对定位、固定高度截断增删对照内容。最终排版继续通过“预览整份简历”查看。
- 图片增删、资源和页面设置变化保留提示，不将纯排版修改误报为没有差异。

审阅仅投影已有完整文档，不持久化第二份文档，也不修改模型、提示词、正文或建议。预览接口按同一 revision 返回当前文档及合并目标；原应用、再次应用、预览及导出边界不变。

## 验证

执行：

```sh
node --test tests/resume-review.test.js tests/resume-review-browser.test.js tests/resume-change-preview.test.js tests/proposal-diff.test.js tests/proposal-preview-browser.test.js
```

30 项通过，0 失败、0 跳过（日志 `/tmp/resume-review-image-final-20260910.log`）。使用真实 Chromium 与隔离测试数据库，模型返回注入固定数据；不代表进行了新的真实 Astra 质量评估。

覆盖：

- 完整正文、单行替换、增删、非 editable 可见文字、XSS 文字转义。
- 任意层级删除、分组/移出分组、顺序变化、整份新 ID 重构，多段编辑内容；编辑容器保留或删除时后代移出不误标删除，编辑内容中的图片不丢失，根 ID 重建保留根自身文字对照。
- 可编辑容器内删除图片时保留删除提示及缩略图，移走的图片不误报删除；图片浏览器测试使用合法 PNG，不放宽安全过滤规则。
- 样式详情绑定对应内容，纯样式不空白。
- 真实浏览器列表、表格、绝对定位内容、90 段长简历、390px 窄屏；内容不截断或横向溢出。
- 真实建议差异到完整预览；PDF/Word 实际下载并核对内容；首次/再次应用与预览一致；过期 revision 拒绝；只查看/导出不改变草稿及版本。

主任务已统一完成全量回归：405 项中 399 通过、0 失败、6 项真实模型调用测试未启用（日志 `/tmp/resume-chat-review-verified-20260910.log`），并重启生产服务。新增静态模块 HTTP 200，线上导航与当前正文预览只读复查通过；生产当前没有保留可预览建议，因此没有将隔离的建议差异测试宣称为线上既有建议验证。完整发布检查见 `CHAT_CONTROLS_REGRESSION_2026-09-10.md`。

未提交或推送代码。
