# 简历星球 PRD

- 版本：v2.15.0
- 日期：2026-09-08
- 阶段：MVP
- 真实入口：`index.html`；原型文件仅留作历史参考，不维护、不参与验收。
- AI 行为约束：[SYSTEM_PROMPT.md](./SYSTEM_PROMPT.md)
- AI 验收用例：[AI_BEHAVIOR_TESTS.md](./AI_BEHAVIOR_TESTS.md)

## 变更记录

### 当前生效版本

| 日期 | 版本 | 核心变更 |
|---|---|---|
| 2026-09-08 | v2.15.0 | 首页统一输入、当前简历＋对话、独立制作与返回；聊天文件一次识别、岗位链接读取；首次空白生成自动成版；版本与五步撤销独立；停止原型验收 |
| 2026-09-08 | v2.14.1 | 增删按完整视觉单元执行、显式区分内部段落、跨行表格保护；全局聊天图片上传/粘贴/拖入、多轮看图与清理 |
| 2026-09-08 | v2.14.0 | 简历自然聊天：明确要求直接预览；持续对话、失败重试、跨刷新恢复与停止生成；独立修改保留、建议批次原子提交 |
| 2026-09-08 | v2.13.2 | 新对话删除旧聊天与建议；局部任务结束清理历史；缩减重复缓存与过期撤销正文，保留必要回执和有效撤销 |
| 2026-09-08 | v2.13.1 | 局部保持轻量纯文本；全局保留文字、结构与样式上下文；旧对话滚动记忆、原话始终位于最后，恢复不得擅自改变要求；根样式与纸张设置显示生效 |

### 历史文档决策

以下内容仅用于追溯，不作为当前需求：

| 日期 | 版本 | 当时的核心变更 |
|---|---|---|
| 2026-09-07 | v2.12.0 | 增加语义节点旁轻量 `+/-`；局部 AI“继续调整”以上一版候选为直接输入；模型链增加 Flash/Pro/Vision 能力路由、严格 Schema 与供应商无关错误协议 |
| 2026-09-05 | v2.11.0 | 文件导入统一生成带语义类型和真实父子关系的简历树；AI 读取精简语义投影并只返回稀疏的最小变化 |
| 2026-09-05 | v2.10.0 | 新增内容只返回新节点及其稳定位置；文字修改保留原排版样式，局部选区不猜位置，复杂内容与结构联动先确认思路 |
| 2026-09-05 | v2.9.0 | 全局 AI 改为返回最小变化区域，由服务端重建完整目标草稿；输出预算按简历复杂度动态调整并受硬上限保护 |
| 2026-09-05 | v2.8.0 | 新增正文旁“就地改写”：只改当前文字或选区；与右侧全局 AI 共用完整简历上下文，但请求、建议和界面状态相互隔离 |
| 2026-09-04 | v2.7.0 | AI 沟通统一为自然消息与最终建议两种结果；上下文按用户、项目、会话和任务隔离，继续调整完整携带 A/B/C 与任务对话 |
| 2026-09-04 | v2.6.0 | AI 以完整目标简历表达修改；服务端把建议生成时草稿、目标简历和应用时最新草稿做确定性三方合并，保留无关手工修改 |
| 2026-09-04 | v2.5.0 | AI 请求按简单直出、复杂确认、歧义追问分流；当前简历统一支持五级撤销/重做；一个 AI 编辑单元只对应一个真实编辑节点 |
| 2026-09-04 | v2.4 | AI 先区分回答、澄清和修改建议；真实结果有歧义时在同一任务内追问；曾允许内容组与子项拥有两套编辑身份 |
| 2026-09-04 | v2.3 | AI 按稳定区域边界执行结构调整；建议卡片以语义描述真实变化并与说明合并展示；支持“同一区域、多个可独立编辑内容”的通用分组；模型异常与建议不可执行错误准确区分 |
| 2026-09-04 | v2.2 | 现有文字轻量直改，结构与样式由 AI 提案；建议卡片展示真实修改前后内容；用户点击“应用修改”代表以 AI 建议覆盖当前内容，只有操作客观不可执行时才冲突 |
| 2026-09-03 | v2.1 | 当前简历收敛为唯一完整可编辑文档；曾计划支持 Word 式直接编辑 |
| 2026-09-02 | v1.9 | 文件导入收口为“预览—应用”：正文与当前模板同时保存，导入模板默认不进入收藏；更换模板时再选择是否保存历史版本或收藏当前模板 |
| 2026-09-02 | v2.0 | 用户只管理当前简历和历史版本；文件导入一次应用并自动保存完整版本；内部采用正文、排版结构和绑定关系三部分保存 |
| 2026-09-02 | v1.8 | 文件导入统一为页面场景：保留原页面视觉层，并叠加可编辑文字层；复杂 DOCX 不再依赖逐条模拟 Word 规则 |
| 2026-09-02 | v1.7 | DOCX 导入保留分页、表格、分栏卡片、字号、字体、颜色、边框和间距；PDF/图片按页面坐标重建可编辑结果 |
| 2026-09-02 | v1.6 | “从文件导入”增加 PNG、JPG、WEBP 图片；识别结果必须预览确认后才能应用 |
| 2026-09-02 | v1.5 | 统一“从文件导入”；首期仅支持 PDF、DOCX、DOC；导入与资料分离；明确模板概念、跨模板历史比较和分阶段实现 |
| 2026-09-01 | v1.4 | 建立动态简历正文、资料/简历/对话分离、草稿与不可变历史版本规则 |

正文只保留当前有效要求；日期差异以本表和 Git 记录为准，不在正文中并列维护多套旧规则。

### 当前统一口径

- 用户围绕满意的当前简历与 AI 对话操作；首页统一接收文字、链接和附件，不要求先建个人档案、岗位档案或选模板；
- 首页附件仅上传并显示，点击“开始生成”或回车才提交，Shift＋回车换行；已有简历入口位于首页下方；
- “制作另一份简历”返回首页，可完整复制当前文档作为起点，也可取消复用后提交其他材料。原简历、岗位、对话与历史独立保留；离开前必须完成草稿保存；
- 首次空白生成直接展示并保存成稿及不可变版本；已有/复用文档的 AI 修改仍只提供建议，点击“应用修改”才生效。生成期间用户已改动草稿时退回建议流程，不自动覆盖；
- 文件附件在提交时识别一次，结果作为只读材料继续用于对话，不写入个人档案；原“从文件导入”的预览替换流程继续保留；
- 岗位链接尝试读取公开文本；读取失败在当前流程提示粘贴描述或上传截图。搜索岗位明确在新窗口打开外部搜索，并非站内招聘数据服务；识别到的目标岗位通过独立建议供用户确认，不静默更换；
- 当前简历是一份完整可编辑文档，统一包含内容、页面结构、样式、资源和可选语义标记；
- 文档树的 `children` 是唯一父子关系；标题、段落、列表、表格和模块等语义在导入时统一标记，不因 PDF 或 DOCX 格式不同而分成两套结构；
- 文件导入统一经过“识别—预览—应用”，应用后成为当前简历，并自动保存一个历史版本；
- 用户可直接修改画布中的现有文字；修改自动保存到当前草稿且可撤销，不自动形成历史版本；
- 用户可在正文旁就地改写当前文字或选区；涉及结构、其他位置或资料时转到右侧 AI；
- 当前语义节点旁提供内嵌于简历页面的轻量 `+/-`：光标指向可编辑 DOM 时才显示；增加时递归复制对应的完整参考子树，删除当前完整语义单元；模块标题增加时明确选择“增加模块内容”或“新增同级模块”。移动、合并、拆分、样式、页面和跨区域调整通过 AI 建议完成；
- 表格内默认复制/删除整行，跨行合并单元格关联的多行作为完整行组处理；列表内默认处理整项，经历标题处理整个经历块。只有明确选择“仅增加/删除内部段落”才修改原单元内部。保留任意深度的子树、文字及样式，不固定复制层数；
- 全局聊天的“图片”支持选择、粘贴和拖入 PNG/JPG/WEBP；发送前可预览、移除，支持只发图片。图片不自动导入资料，也不自动修改简历；当前任务后续追问继续携带历史图片，模型输出仍走建议预览与应用。每条最多 8 张、单张不超过 20 MB，上传失败明确提示；
- 新增模块或段落时只生成新增内容和准确位置，现有大段正文不重复生成；用户看到的仍是完整、可确认的实际修改；
- AI 建议卡片根据文档实际变化展示修改前后内容，不向用户展示 DOM 操作名称；
- 形成最终建议前，AI 的回答、追问和处理思路按自然语言展示；只有最终建议进入文档校验和应用流程；
- 对话按用户、项目、会话和任务隔离；开始新对话不会污染或复用旧任务上下文；
- 用户目标会导致不同最终结果时，AI 先提出一个日常语言问题；节点、锚点和执行顺序等内部问题不得转交用户；
- 产品不提供 Word 编辑器、人工/AI 模式切换或通用结构工具栏；`+/-` 只承担结果明确、可撤销的就地语义增删；
- 导入简历不自动改变资料；保存到资料必须单独展示内容并再次确认。

## 1. 产品定义

简历星球是一款面向求职者的 AI 简历制作工具。用户只需要理解当前简历和历史版本：当前简历允许随手修正现有文字，AI 负责更复杂的内容与结构调整；历史版本是不可修改、可复制继续编辑的完整简历。用户可以导入已有文件、直接改字或与 AI 沟通完善简历。

### 1.1 用户问题

1. 个人经历散落在多份旧简历中，每次求职都要重复整理。
2. 不知道如何把日常职责转化成专业、量化、有说服力的描述。
3. 针对不同岗位调整简历费时，且容易遗漏关键要求。
4. 简历内容和页面效果没有作为完整文档保存，无法稳定复现过去的结果。

### 1.2 产品目标

- 基础资料可复用于多个岗位，但不是开始写简历的前置条件；
- 用户只通过与 AI 沟通，也能完成和保存一份简历；
- 从岗位截图到可下载简历在 5 分钟内完成；
- AI 只优化表达和组织，不捏造事实；
- 每次生成都保存不可变版本，可回看、下载和复制。

### 1.3 本期不做

招聘网站直接投递、企业人才库、多人协作、公开模板市场、在线作品集、面试模拟和支付闭环不属于 MVP。

## 2. 目标用户与场景

核心用户是有 1—10 年经验、正在求职或转岗、具备真实经历但不擅长简历表达的职场人，以及需要针对多个岗位快速制作不同版本的用户。

典型场景：用户导入已有简历，确认重建结果后可直接修正文字，并让 AI 增删模块、调整结构或优化表达；完成后下载 PDF 或 Word，并将完整文档保存为可回看和复制继续修改的历史版本。

## 3. 核心概念

| 概念 | 定义 |
|---|---|
| 资料 | 用户可持续维护、可选复用的基础信息，包括个人信息、优势、经历、教育和技能 |
| 简历 | 用户正在完成的完整可编辑文档，包含内容、页面结构、样式、资源和可选语义标记 |
| 文字直改 | 点击当前简历中的现有文字直接修正，自动保存到当前草稿 |
| 就地改写 | 在正文旁对当前编辑单元或所选文字生成轻量 AI 建议，不调整文档结构 |
| 就地语义增删 | 通过节点旁 `+/-` 增加同级内容或删除当前语义单元，标题增加提供两个明确选项 |
| AI 结构调整 | 通过对话移动、合并、拆分模块，或调整段落、样式和页面结构，应用后写入当前草稿 |
| 从文件导入 | 将 PDF、DOCX、DOC 或简历图片识别为完整简历，经预览确认后成为当前简历并自动保存一个历史版本 |
| 岗位信息 | 原始文件、OCR 文本、用户确认文本和结构化分析 |
| 简历项目 | 围绕一个求职方向组织的工作空间 |
| 生成任务 | 一次异步 AI 生成请求 |
| 当前草稿 | 自动保存的可编辑简历状态，不等同于历史版本 |
| 简历文档树 | 当前草稿唯一的编辑与渲染基准；节点有稳定 ID、语义类型和 children 父子关系，不预设模块清单 |
| AI 对话 | 用户说明写作目标、补充本次信息并与 AI 迭代简历的沟通空间，不自动沉淀为资料 |
| 简历版本 | 某个时间点保存下来的完整简历文档及必要上下文，创建后不可修改 |

## 4. 信息架构

- 首页：统一输入、可移除附件、外部岗位搜索、开始生成、我的简历列表；
- 编辑顶栏：我的简历、制作另一份简历、预览/下载、自动保存状态、保存为版本与历史版本；
- 不再设置固定左栏；旧资料和接口保留，不要求用户独立维护；
- 中央：可直接改字并就地调用局部 AI 的简历画布；画布顶部提供悬浮编辑栏，常驻撤销、重做、缩放和文件导入，滚动后自动收紧但不改变按钮位置；
- 右栏：负责结构调整、跨位置联动、资料沟通和整份简历处理的 AI 对话；
- 轻量材料入口：上传文件、补充经历、提供或更换岗位；个人资料与岗位的存量接口继续保留；
- 居中浮层：整份预览、文件导入和历史版本，复用现有完整文档渲染器；
- 产品不提供模板或排版选择器；聊天附件用于生成建议，“从文件导入”用于明确替换当前文档；
- 辅助资料变化不自动修改简历；如可能改善当前正文，由 AI 另行提出修改建议。

点击正文只打开直接编辑和就地改写入口，不暗中改变 AI 的作用范围；只有用户选择“在 AI 对话中处理”时才切换到对应的 `@简历 · 具体内容`。窄屏下 AI 对话通过浮动入口打开；资料不再占据顶部卡片。

## 5. 核心流程

### 5.1 主流程

1. 首页组合提交经历、目标、岗位链接/描述或附件，不强制同时提供岗位与旧简历；
2. 创建独立简历，进入当前简历与 AI 对话；附件识别完成后将原始要求交给同一个全局 Harness；
3. 信息够用时直接生成；只有缺失信息会实质影响成稿时简短追问，用户在同一对话继续回答；
4. 空白首份生成成功自动保存成稿与历史版本，提供真实整份预览；复用稿和已有文档保持建议—预览—应用边界；
5. 不满意可直接要求更简洁、换排版、突出经历或调整结构；不统一要求继续上传资料；
6. 直接改字、语义增删与 AI 应用共享同一草稿、自动保存及最近五步撤销/重做；
7. 可主动保存历史版本；保存版本不清除有效撤销，当前草稿下载不额外创建版本或持久副本；
8. “制作另一份简历”离开前完成保存，不重置或覆盖原稿；新份独立保存岗位、对话和历史。首页列表可返回已有简历。

### 5.2 异常流程

- 麦克风无权限：说明原因，保留键盘输入并提供重新授权。
- 网络中断：本地暂存编辑内容，恢复后自动重试。
- OCR 失败：保留原图，可重新识别或直接粘贴文字。
- 简历文件识别失败：保留原文件和诊断结果，可重新识别、换文件或退出导入；不得改变当前简历和资料。
- 页面重建失败：展示不支持原因，可换文件或使用已成功识别的内容继续编辑。
- 生成失败：保留本次输入和错误原因，不扣额度，支持重试。
- 部分格式失败：标记为“部分成功”，可下载成功格式并单独重试。
- 重复点击：自动合并为同一次请求，不创建重复任务或版本。

## 6. 功能需求

### 6.1 按需复用的存量个人信息

以下为兼容资料管理的字段规则，不是首页生成的准入条件。首页与聊天输入不自动写入此档案；新流程不展示模拟语音能力。

| 分组 | 字段 | 规则 |
|---|---|---|
| 基础信息 | 姓名、头像、手机、邮箱、城市、当前职位、工作年限 | 姓名必填；手机或邮箱至少一项 |
| 个人优势 | 自我概述、核心竞争力 | 0—500 字 |
| 工作经历 | 公司、职位、起止时间、职责与成果 | 至少一段，可排序 |
| 项目经历 | 项目、角色、时间、描述 | 可选，多段 |
| 教育经历 | 学校、专业、学历、时间 | 至少一段 |
| 技能证书 | 技能、熟练度、证书、语言 | 可选 |
| 作品链接 | 名称、URL | 可选 |

编辑规则：

- 输入停顿 800 ms 后自动保存；
- 顶栏展示“保存中 / 已保存 / 保存失败”；
- 经历支持新增、删除、排序和折叠；
- 离开页面时如有未保存内容需提醒；
- 关键删除可撤销。

语音录入：

- 支持优势、职责成果、项目描述等长文本；
- 展示录音中、转写中、完成、失败状态；
- 支持暂停、继续、取消；
- 转写文字插入编辑框，由用户确认；
- MVP 支持普通话，单次最长 5 分钟。

AI 润色：

- 支持“更专业、强调成果、更简洁”三种意图；
- 并排展示原文、建议与差异；
- 用户确认前不得覆盖原文；
- AI 可按要求主动补充数字、公司、项目、技能等内容，不做事实真实性校验；用户通过差异预览决定是否应用；
- 应用后可撤销一次。

验收：刷新后已保存内容不丢；录音状态明确；AI 建议不自动写入；完整度实时计算。

### 6.2 从文件导入与文字直改

文件导入：

- 主界面统一使用“从文件导入”；Web 与移动端共用同一流程；
- 支持 PDF、DOCX、DOC、PNG、JPG、JPEG、WEBP，单文件最大 20 MB，面向 1—5 页正常简历；
- 识别结果先与原文件并排预览，低置信度、遗漏、字体替换、阅读顺序和溢出问题必须明确提示；
- 用户确认前不得改变当前简历；确认后将内容、页面结构、样式和资源一次写入完整简历文档，并自动创建唯一的“文件导入”历史版本；
- 导入只执行一次文档识别。此后文字直改与 AI 协作始终读取同一份当前草稿，不得再次识别；
- PDF、DOCX、DOC 和图片都必须归一化为同一语义节点树；格式差异只保留在页面与样式字段，不得改变后续增删改的结构协议；
- 加密、损坏、包含不可安全处理内容或超过限制的文件必须明确拒绝。

文字直改与 AI 调整：

- 现有可编辑文字始终可点击修改，不设置“人工编辑 / AI 编辑”切换按钮；
- 支持输入法、选择、粘贴和连续输入；停顿或失焦后合并为一次有意义的文字事务；
- 文字事务立即更新当前草稿、自动保存，并与已应用的 AI 修改共同进入最近五步撤销/重做栈，但不自动进入历史版本；
- Enter 不直接改变段落结构；轻量新增和删除使用节点旁 `+/-`。移动、合并、拆分、项目符号、表格、图片、分页、样式和布局由 AI 生成受控建议；
- 模块清单不固定，用户可要求 AI 新增“技能证书”等任意安全内容；
- 用户在 AI 返回前后继续修改同一处文字，建议仍保持可应用；点击“应用修改”表示接受 AI 结果覆盖应用时的当前文字，撤销应恢复到应用前的最新手工内容；
- `@整份简历` 是发起入口，选中具体内容只是语义焦点；全局AI读取完整文字、真实节点、层级、富文本、样式和页面布局，不摘要正文，只省略二进制资源及编辑器临时数据；模型默认返回最小修改，服务端继承未返回字段形成完整目标；
- 正文旁局部 AI 不发送简历树或节点 ID，只发送完整可见正文、可选资料、岗位和当前锁定文字的纯文本；每轮用户要求和上一版建议按自然对话顺序传入，继续调整直接改写最近一版建议；
- 服务端保存建议生成时草稿 A 和目标简历 B；用户点击应用时，将 A→B 的真实变化合并到最新草稿 C，得到最终草稿 D；
- AI 未修改的字段和节点保留 C；用户点击应用后，AI 明确修改的字段采用 B，同一处同期手工改字不阻止应用，撤销恢复应用前的 C；
- 只有 AI 要修改的节点或父位置已不存在、新增节点 ID 被占用、合并形成非法结构等客观情况才不能应用，并提供“按最新内容重新生成”；
- AI 只有生成了可执行修改动作时才能提示用户确认并展示“应用修改”；动作未形成可用结果时优先给出面向结果的简短引导选项，只有模型服务或协议确实失败时才展示失败信息；
- AI 回复只有两种产品结果：自然沟通 `message` 与最终修改建议 `proposal`；复杂处理思路和必要追问都属于 `message`，不再建立额外回复类型；
- AI 必须根据完整语义明确本轮允许改变内容、结构、样式和哪些区域；用户只要求拆分、合并、列表化或移动时，系统必须校验原文字完整保留，不能顺带精简或漏掉内容；
- 一个 AI 编辑单元必须对应一个真实内容节点。该节点内部可保留多个段落及行内格式，但内部段落不再是独立编辑节点；若三个段落需要分别使用 AI，则外层只能作为普通排版容器，三个段落分别成为编辑节点。禁止父子同时拥有编辑身份；
- 区域授权必须支持相邻节点范围、前后边界和原位置内的包裹、拆分与插入，不能因后续内容的自然位移误报越界；
- 建议说明、目标简历和修改预览必须分离：服务端形成的完整目标简历是建议依据，预览由真实文档比较生成，并将节点增删归纳为“合并、拆分、分组、移动”等用户可理解的结果；
- 带待确认动作的助手回复和修改预览显示在同一个建议卡片组中，不重复显示为互相割裂的气泡与卡片；
- 模型不可用、输出无法解析、建议无法执行和变更越界必须返回不同错误，不得统一声称“AI 没有返回结果”；
- 普通问答、单点文字修改和局部首轮改写使用快速文本模型；结构、样式、跨区域、整份简历和协议恢复使用复杂模型；图片请求才使用视觉模型。业务层只依赖稳定能力类型，不硬编码供应商品牌、模型名或供应商错误码；
- 所有生产模型响应必须先通过供应商严格 JSON Schema。全局建议使用固定严格外壳承载序列化业务结果，再由服务端继续校验内部 proposal/actions、稳定节点、真实父子关系、编辑边界和可执行性；严格外壳不得绕过现有领域校验；
- 保存失败可重试，revision 冲突不得静默覆盖。

简历文档规则：

- 当前简历是唯一完整文档，包含内容、页面设置、结构、样式、资源和可选语义标记；
- 节点类型由安全 `semantic.kind` 表达，真实层级和顺序只由 `children` 表达；`parent_id` 仅可按需从树派生，不得持久化双写；
- 产品不建立模板、模板版本、排版预设或正文到槽位的绑定关系；
- 页面、分栏和模块归属由文档树及节点布局属性直接表达；
- 渲染、AI 修改、历史比较、PDF 和 Word 导出均读取同一份简历文档；
- 从历史版本继续修改时复制完整文档，不提取或复制模板。

质量规则：

- 原生解析、OCR 与视觉分析必须交叉校验，不允许模型补写文件中不存在的内容；
- 姓名、电话、邮箱、日期、数字、公司和职位存在不确定时必须进入确认；
- 扫描件无法可靠分离背景文字时必须提示用户，不能伪装成完全可编辑；
- 识别失败或未通过质量检查时不得提供直接应用入口；
- 文件仅本人可见，默认不用于模型训练，用户需确认拥有合法使用权。

验收：支持的文件必须经过识别和预览确认；应用后得到一份完整简历并只新增一个 imported 版本；现有文字可直接修改且不需要模式切换；结构和样式调整由 AI 提案，应用后可保存、撤销、比较和导出。

### 6.3 岗位信息

导入方式：

- 点击选择一张或多张图片；
- 拖拽图片；
- 选择文件夹；
- 移动端拍照；
- 直接粘贴岗位文字作为兜底。

支持 JPG、JPEG、PNG、WEBP，单图最大 10 MB，一次最多 20 张。

OCR 与分析：

- 多图按文件名自然排序，并允许拖动调整；
- 同时保存 OCR 原文和用户修订文本；
- 自动拼接跨图文本并去除重复段落；
- 提取岗位名称、公司、地点、经验、学历、职责、硬技能、软技能和优先条件；
- 输出匹配分、已覆盖能力、缺失能力和优化建议；
- 分析必须忠于用户确认的岗位文本和当前可用内容，不展示或维护证据关系；
- 明示匹配分不等于真实录用概率。

验收：拖拽、选择文件和文件夹均能导入；OCR 可编辑；修改岗位文本后可重新分析；失败不阻断手工粘贴。

### 6.4 一键生成

前置条件：

- 当前草稿已包含姓名、联系方式以及完成本次简历所需的核心经历；这些内容可以来自资料，也可以由用户在对话和正文中补充；
- 当前简历文档可正常编辑和渲染；
- 岗位文本存在且已确认；
- 用户有可用额度或试用权益。

生成原则：

- AI 只能重组、压缩、润色资料、当前正文和用户当前沟通中明确提供的内容；
- 针对岗位关键词调整内容优先级；
- 优先使用可量化成果；
- 遵守当前文档的页面设置，并明确提示溢出或分页变化；
- 先输出结构化 ResumeDocument 操作，再渲染 PDF 和 DOCX；
- 输出调整说明；信息不完整或含义不明确时先向用户提问，不生成无法确认的内容。

状态流：校验中 → 冻结本次输入 → 排队中 → 分析岗位 → 重组履历 → 渲染简历 → 质量检查 → 成功 / 部分成功 / 失败。

结果支持在线预览、下载 PDF/DOCX、查看调整说明、复制历史版本继续修改、更新资料后重新生成。用户离开页面后任务继续，返回时恢复进度。

### 6.5 草稿、版本与历史

保存分为两层：

- 当前草稿：每次文字直改或应用 AI 修改后自动保存，可撤销，不自动进入历史列表；
- 历史版本：由用户点击“保存为版本”创建；一键生成成功和确认应用完整文件导入时必须各自动创建一个。

创建版本时冻结：

- 个人资料完整 JSON；
- 岗位上传文件、确认文本和结构化分析；
- 完整 ResumeDocument、相关资源、PDF/DOCX 引用及校验值；
- 修改摘要、模型与策略版本、创建时间、状态和错误信息。

规则：

- 历史版本不可修改，项目内版本号单调递增；
- 已有简历的 AI 修改须点击“应用修改”；首页授权的首次空白生成直接保存草稿和不可变版本；
- 保存浮层允许修改版本名称，并列出本次尚未成版的修改；
- 撤销修改同步移除对应的待保存状态；
- 离开项目前先完成自动保存，失败则留在当前页并保留输入；不因尚未手动成版阻止制作另一份简历。覆盖当前草稿的历史版本复制仍需明确确认；
- 历史列表只展示文件导入、生成或用户主动保存的版本，不展示每次聊天和未应用建议；
- 历史列表明确区分“文件导入”“手动保存”和“AI 生成”；时间统一根据 `created_at` 动态显示，不保存会随日期失真的“今天”文案；
- 历史记录属于简历项目，每个版本冻结当时的完整文档，后续编辑不会改变旧版本；
- 历史列表为每个版本展示其第一页真实缩略图，用于识别排版、内容密度和页面结构；加载失败时才显示通用占位图；
- 历史采用居中浏览器，详情必须用冻结的完整文档树渲染，自定义模块、样式和页面结构均原样可见；
- “与当前草稿对照”分别展示内容、结构、样式和页面效果；差异过大时并排展示完整文档，不伪装成连续变更记录；
- 历史详情中的“主要调整”只说明创建该版本时纳入的修改，不等同于与当前草稿的差异；
- 历史版本可分别下载 PDF 和 Word；
- 移动端即使收起顶栏次要按钮，也必须在简历工具栏保留清晰可见的历史版本入口；
- 简历编辑栏只在中央画布范围内悬浮；桌面端跟随画布内部滚动，移动端避开品牌栏和资料卡片，保存状态与高频操作不得因滚动消失；
- “从此版本继续修改”复制该版本的完整 ResumeDocument；不提供只复制模板或排版的分支；
- 当前草稿存在未保存修改时，不得静默覆盖；用户必须先保存当前草稿，或明确放弃这些修改；
- 复制旧版本创建新草稿，不覆盖原版本；恢复动作本身不创建新历史版本，再次保存时才产生新版本；
- 失败生成保留内部诊断记录，但不伪装成成功版本。

### 6.6 AI 对话与独立资料

当前简历是核心成果，AI 对话是主要操作入口。资料可以按需复用，但不要求先独立维护，也不再长期占据固定界面。简历可以参考资料，也可以完全基于用户与 AI 的沟通完成；系统不建立资料到正文的派生、绑定或来源关系。

#### 对话作用范围

- 默认作用范围为 `@整份简历`；
- 按需资料使用 `@资料 · 具体对象`，正文使用 `@简历 · 具体内容`；
- 用户从正文选择“在 AI 对话中处理”后，切换为对应的 `@简历 · 具体内容`；
- 删除具体范围后恢复为 `@整份简历`；
- 新范围替换旧范围，不同时维护多个隐含修改对象；
- 每次请求锁定发送时的范围，处理中切换范围不得改变该请求的目标；
- 每轮都向 AI 提供完整当前简历；`@简历 · 具体内容` 是帮助理解“这里、这个模块”的语义焦点，不是 DOM 写权限边界。用户要求确实涉及合并、拆分、移动或联动其他内容时，AI 可提出跨节点操作，但不得顺带修改无关内容；
- `@资料 · 具体对象` 与 `@整份简历` 仍用于区分资料动作和简历动作，不得借简历请求静默写入资料。

#### 就地改写

- 入口位于当前文字旁，可修改整个编辑单元或同一段内的选中文字；
- 选中文字后的快捷工具条使用明确的 AI 智能编写标识；作用范围与操作按钮分层展示，选区摘要保持单行省略，不得把整段原文拼进按钮或撑满画布；
- 输入浮层中的当前文字默认最多展示两行，同时显示字数；长内容提供“展开/收起”，不挤压输入区和主要操作；
- 每次请求读取完整当前简历、可选资料和当前岗位，但模型只能返回锁定文字的替换结果；
- 用户明确提出“不超过 80 字”等可程序判断的字数上限时，服务端按界面一致的字符规则校验真实结果；超限或与当前待改文字完全相同时自动恢复一次，仍不符合则明确失败，不得显示为已完成；
- 就地改写不进入右侧会话，不创建会话任务，不允许增删、移动节点或修改其他位置；
- 超出边界时以一句日常语言说明，并提供“在 AI 对话中处理”；转入后只预填要求，不替用户自动发送；
- 建议未经“应用修改”不改变正文；取消、切换位置和关闭浮层都不得留下可误应用建议；
- 同一位置连续生成时，以用户发起顺序确定最新建议，不受模型返回快慢影响；
- 节点模式下，最后确认的结果覆盖该处当前文字；选区模式只替换原选区在最新文字中的对应位置；
- 全局与局部建议可以同时存在。用户依次确认时，后确认的结果作用于当时的最新草稿；只有目标已删除、选区无法定位或结构身份变化时才阻止应用；
- 应用后自动保存到当前草稿并进入统一五级撤销/重做，不自动创建历史版本。

同一文档编辑：

- 中间画布、文字直改、AI 建议、历史查看和导出共用同一份 ResumeDocument；
- 工作经历、项目经历、姓名和技能不是写死区域；现有文字可直改，任意安全节点可交给 AI 修改；
- 结果明确的同级新增和当前单元删除可直接使用 `+/-`；移动、合并、拆分、重命名和复杂模块编排由 AI 提案，例如新增并完善“技能证书”；
- 文字直改写入当前草稿；已有简历的 AI 建议未经“应用修改”不得写入，首页授权的首次空白生成除外；
- 两种修改方式使用同一组稳定节点 ID、revision、自动保存和撤销机制，不重新执行文档识别；
- 左箭头按最新顺序撤销，右箭头按相反顺序重做，最多保留最近五步；撤销后产生新修改时清空重做分支。

#### 信息与动作处理

| 用户表达 | 资料处理 | 简历处理 | 用户控制 |
|---|---|---|---|
| 润色、压缩、调整语气、顺序或段落 | 不更新 | 生成修改建议 | 点击“应用修改”后写入草稿 |
| 在对话中明确补充经历、成果、数字、职责或技能 | 不自动更新 | 可直接用于本次修改建议 | 应用简历与保存资料相互独立 |
| 明确要求长期保存某项信息 | 生成资料保存建议 | 不自动改变 | 用户确认后写入资料 |
| 同时要求“写进简历并保存资料” | 生成两个独立建议 | 展示简历修改建议 | 两个动作分别确认、分别撤销 |
| 用户要求 AI 补充、推测或量化内容 | 不更新 | 直接生成可应用建议 | 用户通过差异预览决定是否应用 |
| 新岗位或岗位关键信息变化 | 生成岗位切换建议 | 不自动改变当前简历 | 点击“设为当前岗位”后生效 |
| 假设、举例或临时讨论 | 不更新 | 仅用于当前回答或建议 | 不自动持久化 |

用户在当前对话中明确说出的信息，无需先保存到资料即可用于简历。AI 也可按要求主动补充或推测内容；系统不判断其真实性，用户在差异预览中拥有最终决定权。资料保存、岗位切换和简历应用始终是互不替代的独立动作。

#### 多轮沟通与建议顺序

- AI 先理解用户目标，再按需要回答、追问或提出修改，不要求用户选择固定功能流程；
- “把这句话写得更简洁”等简单且明确的请求直接生成建议，摘要说明保留原意、数字等约束；
- 明确要求直接生成可预览建议，即使涉及跨模块、文字、结构、样式联动也不增加思路确认；用户主动要求讨论时才先讨论，已确认思路直接执行；
- 直接输入下一句话承接当前对话。普通问答不作废上一版，应用后继续读取最新正文。失败可以一键重试原要求；刷新能恢复后台等待；用户可停止生成。聊天不重复展示错误诊断或要求用户重述明确需求；
- 只有不同理解会改变最终结果时才追问，问题保持一个、选项保持 2—3 个；不得询问节点、锚点和执行顺序；
- 追问与用户回答属于同一任务；用户回答后继续原目标，不重新创建一条互不相关的建议；
- 服务端按 `owner → project → conversation → task` 隔离消息与状态。模型接口即使无持久会话能力，服务端也必须为每轮重建当前任务所需上下文；
- 正文真实内容记为 A、当前展示的完整建议态文档记为 B、继续调整后的建议态文档记为 B2；正文未应用前始终是 A。继续调整时必须把完整 B 交给 AI 生成 B2，不能重新读取 A 后判断“无需调整”；
- 继续调整必须同时提供 A、B、最新草稿 C、原始目标和任务内全部有效对话；模型直接调整 B，C 只用于理解用户同期手工修改和最终预览；
- 应用时服务端从完整 A 与最新目标 B2 计算 AI 的真实变化，再确定性合并到 C 得到 D；这一阶段不再调用模型；
- 每次只保留一条当前可应用建议。新建议出现后，旧建议仅供查看，不形成用户可见分支或正文派生树；
- 同一任务产生新建议后旧建议必须失效；草稿 revision 或普通文字变化不使目标简历建议失效，只有客观结构冲突才阻止应用；
- 用户补充的新信息可以直接进入下一版建议；如果含义仍有歧义，AI 先追问，不创建待确认资料；
- 本轮用户要求优先于历史消息。AI 不得只重复上一轮结果而忽略最新要求；
- 界面中的“当前内容”始终指真实正文；建议需明确标识为尚未应用。
- 文字、合并、拆分、新增、删除、移动、样式和混合修改共用同一套差异预览；用户手工修改后，待应用建议按最新草稿重新计算预览。
- 多个要点包裹进显式内容组后，默认由内容组整体承接 AI 请求；当用户要求分别使用 AI 时，只取消整体 AI 联动即可保留当前排版和子项文字。只有用户明确要求改变物理结构时才拆除内容组。

#### 资料保存

- 用户可以随时进入资料页新增、修改或删除内容；
- AI 可以提出“保存到资料”的建议，但不得自动执行；
- 对话中未保存到资料的内容只服务当前对话和简历修改，不形成待确认资料队列；
- 资料修改不自动改写简历；如可能改善当前正文，AI 另行提供修改建议；
- 简历修改不自动回填资料；如适合长期复用，AI 可单独询问是否保存。

#### 开始新对话

- AI 区提供“开始新对话”，操作前说明未应用建议将失效；
- 当前聊天和追问任务结束，创建空对话并恢复为 `@整份简历`；
- 新对话创建成功后，同一事务删除本项目旧消息、任务记忆和旧建议，不只是隐藏；
- 未应用的简历、资料和岗位建议失效，不能跨对话继续应用；
- 已保存资料、当前岗位、完整简历文档、草稿修改、撤销记录和历史版本均不改变。
- 局部修改应用成功或明确放弃后，删除该任务问答及建议正文；生成或应用失败时保留重试所需内容。迟到结果不得恢复已清空任务。
- 最近五步撤销/重做与已保存版本独立保存；失效操作只保留简短记录，重复提交缓存不保存整份简历。

#### 更新反馈与独立性

- 资料保存、岗位切换、应用修改和撤销成功后，均展示真实执行结果；
- 修改资料不自动改变简历，修改简历不自动改变资料；
- 岗位切换后重新分析匹配关系，只生成调整建议，不自动重写整份简历；
- 操作记录只用于撤销、并发控制、安全审计和问题排查，不承担内容来源或派生关系。

#### 不可破坏的产品约束

1. AI 模型不得直接写数据库、调用资料更新接口或修改浏览器中的正文；模型只能提出锁定文本或受控正文树操作，由后端校验并在用户确认后执行；
2. AI 声称“已保存”不代表保存成功，界面只显示后端真实执行结果；
3. AI 可按用户要求补充或推测简历内容，服务端不进行事实真实性判断，所有结果必须先展示差异并由用户决定是否应用；
4. 未点击“应用修改”时，中间简历正文必须保持不变；
5. 未确认“保存到资料”时，当前对话内容不得自动进入资料；
6. 未点击“设为当前岗位”时，当前岗位必须保持不变；
7. 资料、简历、岗位和版本之间不得建立来源、证据映射或派生树；
8. 所有写入动作必须经过后端权限、revision、幂等和目标校验，并可按产品规则撤销；
9. 无法确定目标、Schema 不合法或状态冲突时一律不写入，并向用户说明。

验收以 [AI_BEHAVIOR_TESTS.md](./AI_BEHAVIOR_TESTS.md) 的 P0 用例为准；P0 未达到 100% 不得发布。

## 7. 状态与权限

| 对象 | 状态 |
|---|---|
| 自动保存 | idle / saving / saved / failed / conflict |
| 上传文件 | selected / uploading / scanning / parsing / ready / failed |
| 简历文件识别 | uploaded / scanning / normalizing / extracting / analyzing / validating / needs_review / ready / applied / failed |
| 语音 | idle / recording / transcribing / completed / failed |
| 生成任务 | validating / queued / running / partial / succeeded / failed / canceled |
| 当前草稿 | saved / unsnapshotted / saving / failed / conflict |
| 历史版本 | complete / partial |
| 内部生成快照 | pending / complete / failed |

MVP 为个人账号模式。用户只能访问自己的项目、文件、草稿和版本。管理员默认只看到脱敏诊断信息。

## 8. 交互规范

- 按钮使用“动作 + 对象”，如“应用修改”“选择图片”；
- AI 操作统一用 ✦ 标识；
- 保存失败、上传失败不能只靠颜色表达；
- 上传区可点击、拖拽和键盘操作；
- 所有进度均提供文字状态；
- 删除、覆盖、取消生成须二次确认；
- AI 建议与当前正文清楚区分；任何已应用修改都可撤销。

## 9. 指标与埋点

北极星指标：每周至少完成一次岗位定制简历生成的活跃用户数。

漏斗：创建项目 → 导入或新建简历 → 文字直改或 AI 协作 → 发起生成 → 成功生成 → 下载。

质量目标：

- OCR 成功率 ≥ 95%；
- 合格文字版 PDF、DOCX、DOC 的可见文本完整率 ≥ 99.5%；
- 简历文件识别不得静默遗漏或编造关键字段，所有不确定结果进入用户确认；
- 生成任务成功率 ≥ 98%；
- P95 生成时间 ≤ 60 秒；
- 自动保存成功率 ≥ 99.9%；
- AI 建议预览后的应用率与继续修改率可观测。

关键事件包括 profile_saved、voice_transcribed、polish_applied、document_import_started、document_import_reviewed、document_import_applied、inline_text_edit_started、document_transaction_saved、inline_text_edit_completed、ai_structure_proposed、job_ocr_completed、job_analysis_completed、generation_started、generation_succeeded、version_saved、version_opened、version_compared、resume_downloaded。

## 10. 优先级

### P0

个人信息 CRUD、自动保存、语音转写、AI 润色、现有文字轻量直改、AI 结构调整、PDF/DOCX/DOC/图片统一导入与预览确认、完整 ResumeDocument 重建、岗位图片 OCR、一键生成 PDF/DOCX、主动保存版本、不可变版本、历史详情/对照/完整复制和失败重试。

### P1

复杂表格、文本框、浮动图片和多页文档编辑、文件夹上传与多图排序、版本数据包导出、移动端拍照优化。

### P2

扫描型 PDF、特殊字体和可视化校正、更多语言、岗位链接解析、团队协作和面试辅助。

## 11. 发布验收

1. 新用户不看说明可在 5 分钟内完成一次生成；
2. 缺少必需素材时，生成入口能准确定位问题；
3. 每次成功生成有且只有一个内部生成快照和一个用户可见版本；
4. 主动保存或生成的版本均可还原当时三类输入和简历结果；
5. AI 不把推测、示例或岗位要求写成用户经历；
6. 语音、OCR、文件文档识别和生成失败均有恢复路径；
7. PDF 与 DOCX 语义一致，中文字体不缺失；
8. 导出文件不泄露对象存储地址或内部元数据；
9. 用户 A 无法访问用户 B 的资源；
10. 主流桌面浏览器和移动端核心流程可用。
11. 简历修改不会反向写入资料，资料修改也不会自动改写简历；
12. 用户在对话中明确补充的内容无需先进入资料即可用于简历建议；AI 推测的信息只能追问，不能进入可应用建议；
13. 保存资料与应用简历是两个独立动作，均显示真实执行结果并可按规则撤销；
14. 新岗位未经确认不会替换当前岗位；
15. 用户未应用修改时简历正文不发生变化；
16. AI 返回越权动作、非法 Schema 或未知动作时后端拒绝写入；
17. [AI_BEHAVIOR_TESTS.md](./AI_BEHAVIOR_TESTS.md) 中全部 P0 用例通过。
18. 应用 AI 修改只更新草稿，不自动新增历史版本；
19. “保存为版本”只新增一个版本，撤销后不保留错误的待成版修改；
20. 从旧版本继续修改不会覆盖原版本、当前个人信息或其他已保存内容；当前草稿有未保存修改时必须先保存或明确放弃。
21. 历史详情可完整展示任意动态正文模块；比较默认针对实时草稿，不得只比较固定经历字段或忽略尚未成版的修改。
22. PDF、DOCX、DOC 和图片导入结果必须先预览确认；未确认时当前简历和资料均保持不变。
23. 当前简历以单一 ResumeDocument 保存和渲染，不建立模板、排版预设或槽位绑定对象。
24. 文件导入一次应用完整文档且有且只有一个 `imported` 历史版本；导入后的文字直改和 AI 协作不得重新识别。
25. 现有文字无需切换模式即可直接修改、自动保存和撤销；结构、样式和页面操作只能由 AI 提案并经用户应用。
26. 历史版本冻结完整文档；复制旧版本创建独立草稿，不覆盖原版本或建立版本树。
27. AI 建议卡片必须展示系统计算的真实修改前后内容，不得以“新增内容、删除内容”等底层操作名称代替结果。
28. 明确修改直接生成建议；分析与讨论直接回答；只有真实歧义才追问，不强制增加思路确认。
29. 文字直改和 AI 的增、删、移动、结构修改共用最近五步撤销/重做；撤销后新修改清空重做分支。
30. 任一可编辑节点不得包含可编辑后代；合并为一个 AI 编辑节点时可保留多段格式，拆分后父容器不得继续作为 AI 编辑节点。
31. 正文旁就地改写只修改当前编辑单元或同一段选区，不得改变结构、其他位置、资料或右侧会话。
32. 点击正文不得暗中改变右侧 AI 作用范围；只有用户明确转到对话时才切换。
33. 同一位置并发生成以请求发起顺序确定最新建议；切换位置或关闭后，迟到结果不得重新出现或保持可应用。
34. 全局与局部 AI 的确认顺序决定最终结果；应用时保留未涉及位置的最新手工修改。
35. 局部建议应用后进入同一草稿和五级撤销/重做，不自动形成历史版本。
36. 局部入口在桌面与移动端均支持键盘关闭、状态播报、失败重试和明确的当前/修改后预览。
37. 直接编辑或 AI 改写标题、粗体、斜体和混合样式文字时，只改变文字，不得把标题变成正文或清空原格式。
38. 局部选区在相同文字多处出现时必须依据原位置上下文准确应用；无法唯一定位或范围异常变化时正文保持不变。
39. 模型同时返回调试对象、示例对象和最终对象时不得猜选；必须恢复为唯一完整结果后才能进入确认。
40. 全局目标片段只改一句文字时不得返回整个模块或整份根节点；已停用的 AI 范围属性不得通过兼容迁移进入新建议。
41. PDF、DOCX、DOC 和图片导入后必须形成同一语义节点模型；标题、正文和模块父子关系可被后续通用增删改可靠定位。
42. 全局 AI 上下文保留整份简历正文、结构、行内格式、样式、布局及完整页面设置，资源保留描述而不传字节；局部 AI 不发送文档树、样式或节点 ID。两类稀疏结果应用后必须保留未修改区域的原标签、语义、富文本和样式。
43. 文字、结构和样式联动修改与普通改字共用“要求 → 建议 → 应用”流程；模型建议经校验后直接预览，正文直到应用才改变。
44. 局部请求包含明确字数上限时，系统必须校验建议的真实字符数；超限或原样返回只允许自动恢复一次，再次不合格不得进入待应用状态。
45. 全局与局部模型调用都按“系统规则、只读上下文、当前任务历史、本轮 user”组织；任务历史保持真实 `user/assistant` 顺序。全局直接发送本轮原话；局部 working set 的 `instruction` 原样携带本轮要求、`editing_text` 携带唯一编辑对象，且不得在历史中重复。
46. 长对话整理早期任务记忆而非丢弃要求；最近对话和本轮原话不裁剪。整理失败保留原记录和上一版建议，明确提示重试。
47. 全局请求不得因后端关键词猜测而被收窄成局部文字操作；恢复只处理客观协议问题，不把技术提示冒充用户要求。普通短局部任务不增加摘要调用。
