# 简历星球 PRD

- 版本：v1.2
- 日期：2026-08-31
- 阶段：MVP
- 配套原型：[index.prototype.backup.html](./index.prototype.backup.html)
- AI 行为约束：[SYSTEM_PROMPT.md](./SYSTEM_PROMPT.md)
- AI 验收用例：[AI_BEHAVIOR_TESTS.md](./AI_BEHAVIOR_TESTS.md)

## 1. 产品定义

简历星球是一款面向求职者的 AI 简历制作工具。用户维护一份可持续更新的个人履历，选择系统或自有模板，导入目标岗位，即可生成岗位定制简历，并通过历史版本完整回溯当时的输入与结果。

### 1.1 用户问题

1. 个人经历散落在多份旧简历中，每次求职都要重复整理。
2. 不知道如何把日常职责转化成专业、量化、有说服力的描述。
3. 针对不同岗位调整简历费时，且容易遗漏关键要求。
4. 内容、模板、岗位信息分离，无法稳定复现某个历史版本。

### 1.2 产品目标

- 一份个人信息可复用于多个岗位；
- 从岗位截图到可下载简历在 5 分钟内完成；
- AI 只优化表达和组织，不捏造事实；
- 每次生成都保存不可变版本，可回看、下载和复制。

### 1.3 本期不做

招聘网站直接投递、企业人才库、多人协作、公开模板市场、在线作品集、面试模拟和支付闭环不属于 MVP。

## 2. 目标用户与场景

核心用户是有 1—10 年经验、正在求职或转岗、具备真实经历但不擅长简历表达的职场人，以及需要针对多个岗位快速制作不同版本的用户。

典型场景：用户从招聘 App 截取岗位描述，打开简历星球，确认个人信息，选择“经典商务”模板，拖入岗位截图，确认 OCR 结果和关键词，点击“一键生成”。系统输出 PDF 和 DOCX，同时创建包含三类输入及结果文件的历史版本。

## 3. 核心概念

| 概念 | 定义 |
|---|---|
| 个人信息 | 可持续维护的事实型履历，包括基础信息、优势、经历、教育和技能 |
| 模板 | 决定布局和样式的系统模板或用户上传模板 |
| 岗位信息 | 原始文件、OCR 文本、用户确认文本和结构化分析 |
| 简历项目 | 围绕一个求职方向组织的工作空间 |
| 生成任务 | 一次异步 AI 生成请求 |
| 当前草稿 | 自动保存的可编辑简历状态，不等同于历史版本 |
| 版本快照 | 用户主动保存或生成成功时冻结的个人资料、岗位信息、模板和简历结果，创建后不可修改 |
| 简历版本 | 历史列表中的用户可见版本，对应一个版本快照 |

## 4. 信息架构

- 顶栏：品牌、自动保存状态、历史版本、生成简历和账号；
- 左栏：只保留“个人信息”和“岗位信息”两个事实资料模块；
- 中央：简历画布、模板与页面工具，始终展示当前简历；
- 右栏：AI 对话、`@作用范围`、修改方案与确认操作；
- 居中浮层：个人资料、岗位资料、模板选择和历史版本；
- 模板不进入左侧资料库，在中央简历工具栏中选择或上传；
- 左侧资料变化先提示影响范围，除白名单基础字段外不得静默修改中央简历。

点击左侧卡片只打开居中资料浮层，不改变 AI 作用范围；进入 AI 对话后分别使用 `@资料 · 个人信息`、`@资料 · 岗位信息` 或具体资料对象。中间正文使用 `@简历 · 具体内容`，未选内容时为 `@整份简历`。窄屏下资料卡片变为顶部横向卡片，AI 对话通过浮动入口打开。

## 5. 核心流程

### 5.1 主流程

1. 创建或打开简历项目；
2. 键入或语音录入个人信息；
3. 对优势、工作经历等字段发起 AI 润色并确认；
4. 选择系统模板或上传自有模板；
5. 拖入岗位截图，系统 OCR 并分析；
6. 用户修订并确认岗位文本；
7. 系统检查三个模块是否满足生成条件；
8. 用户点击“一键生成”；
9. 系统原子化冻结输入并启动异步生成；
10. 成功后保存结构化简历、PDF 和 DOCX；
11. 系统自动创建历史版本；用户可下载、比较或复制继续修改。

### 5.2 异常流程

- 麦克风无权限：说明原因，保留键盘输入并提供重新授权。
- 网络中断：本地暂存编辑内容，恢复后自动重试。
- OCR 失败：保留原图，可重新识别或直接粘贴文字。
- 模板解析失败：展示不支持原因，可换文件或改用系统模板。
- 生成失败：保留输入快照和错误原因，不扣额度，支持重试。
- 部分格式失败：标记为“部分成功”，可下载成功格式并单独重试。
- 重复点击：通过幂等键合并请求，不创建重复快照。

## 6. 功能需求

### 6.1 个人信息

| 分组 | 字段 | 规则 |
|---|---|---|
| 基础信息 | 姓名、头像、手机、邮箱、城市、当前职位、工作年限 | 姓名必填；手机或邮箱至少一项 |
| 个人优势 | 自我概述、核心竞争力 | 0—500 字 |
| 工作经历 | 公司、职位、起止时间、职责与成果 | 至少一段，可排序 |
| 项目经历 | 项目、角色、时间、描述 | 可选，多段 |
| 教育经历 | 学校、专业、学历、时间 | 至少一段 |
| 技能证书 | 技能、熟练度、证书、语言 | 可选 |
| 作品链接 | 名称、URL | 可选 |

编辑规则：

- 输入停顿 800 ms 后自动保存；
- 顶栏展示“保存中 / 已保存 / 保存失败”；
- 经历支持新增、删除、排序和折叠；
- 离开页面时如有未保存内容需提醒；
- 关键删除可撤销。

语音录入：

- 支持优势、职责成果、项目描述等长文本；
- 展示录音中、转写中、完成、失败状态；
- 支持暂停、继续、取消；
- 转写文字插入编辑框，由用户确认；
- MVP 支持普通话，单次最长 5 分钟。

AI 润色：

- 支持“更专业、强调成果、更简洁”三种意图；
- 并排展示原文、建议与差异；
- 用户确认前不得覆盖原文；
- 不得新增数字、公司、项目和技能；疑似新增事实必须标记待确认；
- 应用后可撤销一次。

验收：刷新后已保存内容不丢；录音状态明确；AI 建议不自动写入；完整度实时计算。

### 6.2 简历模板

系统模板首期提供：

1. 经典商务：单栏、稳重、ATS 友好；
2. 现代分栏：适合互联网与创意岗位；
3. 极简留白：适合专业、研究和管理岗位。

自定义模板：

- 支持 JPG、JPEG、PNG、PDF、DOC、DOCX；
- 单文件最大 20 MB；
- 图片作为单页背景式模板；
- PDF 支持 1—3 页，优先识别文本框和布局；
- Word 转为内部模板描述；
- 解析中展示进度，成功后显示缩略图；
- 加密 PDF、损坏文件、超限文件需准确报错；
- 文件仅本人可见，默认不用于模型训练；
- 用户需确认拥有合法使用权。

验收：三款系统模板可切换；上传后显示文件名、大小和状态；生成时将模板实际版本写入快照，而非只存模板 ID。

### 6.3 岗位信息

导入方式：

- 点击选择一张或多张图片；
- 拖拽图片；
- 选择文件夹；
- 移动端拍照；
- 直接粘贴岗位文字作为兜底。

支持 JPG、JPEG、PNG、WEBP，单图最大 10 MB，一次最多 20 张。

OCR 与分析：

- 多图按文件名自然排序，并允许拖动调整；
- 同时保存 OCR 原文和用户修订文本；
- 自动拼接跨图文本并去除重复段落；
- 提取岗位名称、公司、地点、经验、学历、职责、硬技能、软技能和优先条件；
- 输出匹配分、已覆盖能力、缺失能力和优化建议；
- 每项分析可追溯到岗位原文或个人履历证据；
- 明示匹配分不等于真实录用概率。

验收：拖拽、选择文件和文件夹均能导入；OCR 可编辑；修改岗位文本后可重新分析；失败不阻断手工粘贴。

### 6.4 一键生成

前置条件：

- 姓名、联系方式、至少一段工作/项目经历和一段教育经历；
- 已选择可用模板；
- 岗位文本存在且已确认；
- 用户有可用额度或试用权益。

生成原则：

- AI 只能重组、压缩、润色用户提供的事实；
- 针对岗位关键词调整内容优先级；
- 优先使用可量化成果；
- 控制在模板页数内；
- 先输出结构化 Resume JSON，再渲染 PDF 和 DOCX；
- 输出调整说明、事实证据和待确认项。

状态流：校验中 → 创建快照 → 排队中 → 分析岗位 → 重组履历 → 渲染模板 → 质量检查 → 成功 / 部分成功 / 失败。

结果支持在线预览、下载 PDF/DOCX、查看调整说明、复制历史版本继续修改、更新资料后重新生成。用户离开页面后任务继续，返回时恢复进度。

### 6.5 草稿、版本与历史

保存分为两层：

- 当前草稿：每次应用 AI 修改、直接编辑正文或更换模板后自动保存，可撤销，不自动进入历史列表；
- 历史版本：由用户点击“保存为版本”创建；一键生成成功时必须自动创建。

创建版本时冻结：

- 个人资料完整 JSON；
- 岗位源文件、确认文本和结构化分析；
- 模板类型、版本、内部描述和源文件引用；
- 当前结构化简历、PDF/DOCX 引用及校验值；
- 修改摘要、模型与策略版本、创建时间、状态和错误信息。

规则：

- 版本快照不可修改，项目内版本号单调递增；
- AI 只提出修改方案；用户点击“应用修改”后更新草稿并点亮“保存为版本”；
- 保存浮层允许修改版本名称，并列出本次尚未成版的修改；
- 撤销修改同步移除对应的待保存状态；
- 再次生成、更换岗位、复制旧版本或离开项目前，如有未成版修改，只集中询问一次是否保存；
- 历史列表只展示生成或用户主动保存的版本，不展示每次聊天和未应用建议；
- 历史采用居中浏览器，支持独立详情、查看当时资料、与当前版本比较、下载和复制继续修改；
- 复制旧版本创建新草稿，不覆盖原版本；再次保存时产生新版本；
- 失败生成保留内部诊断记录，但不伪装成成功版本。

### 6.6 AI 对话与资料同步

左侧个人信息与岗位信息是事实资料库，中间简历是基于事实生成的表达，右侧 AI 对话是统一操作入口。三者必须保持职责分离：AI 可以识别和建议操作，但不得自行决定持久化资料或替换简历正文。

#### 对话作用范围

- 默认作用范围为 `@整份简历`；
- 左侧资料使用 `@资料 · 具体对象`，中间正文使用 `@简历 · 具体内容`；
- 用户选择具体内容并点击“帮我改写”后，切换为对应的 `@简历 · 具体内容`；
- 删除具体范围后恢复为 `@整份简历`；
- 新范围替换旧范围，不同时维护多个隐含修改对象；
- 每次请求锁定发送时的范围，处理中切换范围不得改变该请求的目标。
- `@` 只限定本轮允许修改的对象，不限制 AI 读取完成任务所需的相关事实、上下文、岗位要求和同段内容。

#### 信息分类与处理

| 用户表达类型 | 左侧资料处理 | 中间简历处理 | 用户控制 |
|---|---|---|---|
| 文案润色、压缩、调整语气或拆分段落 | 不更新 | 生成差异方案；“改成 2 个段落”等结构数量不作为新事实 | 点击“应用修改”后替换 |
| 明确修改姓名、电话、邮箱、城市等白名单字段 | 更新并记录旧值 | 一一对应字段可同步 | 显示修改回执并支持撤销 |
| 新增业绩数字、项目规模、职责、技能等事实 | 创建待确认资料 | 暂不改变 | 确认保存后再生成修改方案 |
| AI 从语音、文件或上下文推断的信息 | 创建待确认资料 | 暂不改变 | 必须逐项确认或拒绝 |
| 新岗位或岗位关键信息变化 | 创建待确认岗位 | 暂不改变当前岗位 | 点击“设为当前岗位”后生效 |
| 假设、举例、临时讨论 | 不更新 | 仅用于本轮回答 | 用户明确要求保存后重新分类 |
| 意图或目标不明确 | 不更新 | 不修改 | AI 先提问，不得猜测执行 |

一条消息可以同时包含候选事实和修改要求，系统必须拆分处理；依赖候选事实的修改建议需等待相关事实确认或拒绝后再生成。

#### 多轮沟通与建议顺序

- 每个修改目标按独立任务编排；必要信息未补齐时先追问，同一任务一次只处理一个当前问题。
- 只阻塞会影响本次建议的待确认事实，其他待确认资料不影响继续处理。
- 相关事实处理完成后再生成第一版建议；进入纯表达调整后，可以基于上一版建议继续迭代。
- 已有建议后出现新事实时，暂停生成下一版，确认或拒绝该事实后再继续。
- 正文真实内容记为 A、上一版建议记为 B、新建议记为 C：正文未应用前始终是 A，但 C 的表达从 B 继续，事实只能来自已确认资料。
- 同一任务同时只有一条建议可应用；旧建议保留用于查看，正文、事实或岗位变化后，受影响的旧建议必须失效或被新版替代。
- 确认或拒绝事实不得原地改写旧建议。确认后生成包含该事实的新版；拒绝后保留不依赖该事实的建议，依赖它的建议失效并按需重生成。
- 界面中的“当前内容”始终指真实正文；建议迭代需另行说明“基于上一版建议继续调整”。
- 事实确认后生成的新版建议立即成为后续调整的起点；下一轮必须处理用户最新要求，不能只重复说明上一轮已确认事实。
- 新数字缺少单位或对象时先追问具体含义；信息完整后进入待确认，确认前不生成依赖它的新建议。

#### 待确认资料

- AI 识别出的新事实必须在内部记录字段、值、来源和影响范围；
- 待确认资料状态为“待确认 / 已确认 / 已拒绝”；
- 用户未当场处理时，左侧持续显示具体待确认数量；
- 只有已确认资料可以进入可靠事实库并用于正式生成；
- 被拒绝内容不得在同一来源未发生变化时重复提示。

#### 开始新对话

- AI 区提供“开始新对话”，操作前说明哪些内容保留、哪些建议会失效；
- 当前聊天和追问任务结束，创建空对话并恢复为 `@整份简历`；
- 旧消息保留在后台记录中，不作为新对话上下文；
- 未应用的简历建议和未确认岗位建议失效，不能跨对话继续应用；
- 待确认资料继续保留在左侧，之后确认时只保存资料，不重新唤醒旧对话的建议链；
- 已确认资料、当前岗位、简历正文、模板、草稿修改、撤销记录和历史版本均不改变。

普通界面不提供独立“来源”模块。后端保留完整追踪；待确认内容只需用“识别自”简洁说明获取途径。

#### 更新反馈与影响同步

- 明确字段修改成功后展示“旧值 → 新值”和撤销入口；
- 新事实确认后先写入左侧，再提示简历中有多少处可同步；
- 除白名单一一对应字段外，左侧资料变化不得静默改写中间简历；
- 岗位切换后重新分析匹配关系，只生成调整方案，不自动重写整份简历；
- 每次确认、拒绝、应用和撤销均记录来源消息、操作者、时间和前后值。

#### 不可破坏的产品约束

1. AI 模型不得直接写数据库、调用资料更新接口或修改 DOM 正文；
2. AI 声称“已保存”不代表保存成功，界面只显示后端真实执行结果；
3. 推测信息、生成文案和岗位原文不得自动成为个人事实；
4. 未点击“应用修改”时，中间简历正文必须保持不变；
5. 未点击“设为当前岗位”时，当前岗位必须保持不变；
6. 所有自动执行动作必须属于后端白名单、可审计且可撤销；
7. 无法分类、Schema 不合法或策略冲突时一律不写入，并向用户说明。

验收以 [AI_BEHAVIOR_TESTS.md](./AI_BEHAVIOR_TESTS.md) 的 P0 用例为准；P0 未达到 100% 不得发布。

## 7. 状态与权限

| 对象 | 状态 |
|---|---|
| 自动保存 | idle / saving / saved / failed / conflict |
| 上传文件 | selected / uploading / scanning / parsing / ready / failed |
| 语音 | idle / recording / transcribing / completed / failed |
| 生成任务 | validating / queued / running / partial / succeeded / failed / canceled |
| 当前草稿 | saved / unsnapshotted / saving / failed / conflict |
| 历史版本 | complete / partial |
| 内部生成快照 | pending / complete / failed |

MVP 为个人账号模式。用户只能访问自己的项目、文件、模板、草稿和版本。管理员默认只看到脱敏诊断信息。

## 8. 交互规范

- 按钮使用“动作 + 对象”，如“应用修改”“选择图片”；
- AI 操作统一用 ✦ 标识；
- 保存失败、上传失败不能只靠颜色表达；
- 上传区可点击、拖拽和键盘操作；
- 所有进度均提供文字状态；
- 删除、覆盖、取消生成须二次确认；
- AI 内容与用户事实可区分、可撤销、可追溯。

## 9. 指标与埋点

北极星指标：每周至少完成一次岗位定制简历生成的活跃用户数。

漏斗：创建项目 → 完成个人信息 → 选择模板 → 解析岗位 → 发起生成 → 成功生成 → 下载。

质量目标：

- OCR 成功率 ≥ 95%；
- 生成任务成功率 ≥ 98%；
- P95 生成时间 ≤ 60 秒；
- 自动保存成功率 ≥ 99.9%；
- AI 虚构事实投诉率 < 0.1%。

关键事件包括 profile_saved、voice_transcribed、polish_applied、template_selected、template_uploaded、job_ocr_completed、job_analysis_completed、generation_started、generation_succeeded、version_saved、version_opened、version_compared、resume_downloaded。

## 10. 优先级

### P0

个人信息 CRUD、自动保存、语音转写、AI 润色、三种模板、自定义模板上传解析、岗位图片 OCR、一键生成 PDF/DOCX、主动保存版本、不可变版本、历史详情/比较/复制和失败重试。

### P1

文件夹上传与多图排序、自定义模板微调、版本数据包导出、移动端拍照优化。

### P2

更多语言和模板、岗位链接解析、团队协作和面试辅助。

## 11. 发布验收

1. 新用户不看说明可在 5 分钟内完成一次生成；
2. 缺少必需素材时，生成入口能准确定位问题；
3. 每次成功生成有且只有一个内部生成快照和一个用户可见版本；
4. 主动保存或生成的版本均可还原当时三类输入和简历结果；
5. AI 润色、生成都不静默新增事实；
6. 语音、OCR、模板解析和生成失败均有恢复路径；
7. PDF 与 DOCX 语义一致，中文字体不缺失；
8. 导出文件不泄露对象存储地址或内部元数据；
9. 用户 A 无法访问用户 B 的资源；
10. 主流桌面浏览器和移动端核心流程可用。
11. 文案润色不会反向写入个人信息或岗位信息；
12. AI 推断的新事实只进入待确认区，确认前不参与正式生成；
13. 明确基础字段修改显示真实执行回执并可撤销；
14. 新岗位未经确认不会替换当前岗位；
15. 用户未应用修改时简历正文不发生变化；
16. AI 返回越权动作、非法 Schema 或未知动作时后端拒绝写入；
17. [AI_BEHAVIOR_TESTS.md](./AI_BEHAVIOR_TESTS.md) 中全部 P0 用例通过。
18. 应用 AI 修改只更新草稿，不自动新增历史版本；
19. “保存为版本”只新增一个版本，撤销后不保留错误的待成版修改；
20. 复制旧版本不会覆盖原版本或当前未确认资料。
