# 全局聊天、差异与预览回归 · 2026-09-10

本轮在 `feature/resume-first-flow` 保留已有改动继续修复。未维护历史原型，未更换模型，未修改本轮生成提示词、事实规则或既有正文应用确认边界。

## 根因与修复

### 差异与整份预览

- 后端已有样式变化信息，但差异界面此前仅比较文字，纯排版建议看起来没有差异。现在同时列出结构、样式、页面设置等字段的旧值与新值；文字仍按完整行显示增删，不逐字拆分。
- 文字提取覆盖可见但未标记 editable 的节点。展示字段使用文本安全渲染，二进制资源只显示摘要，不复制到差异数据。
- 建议预览新增“应用修改”和 PDF/Word 导出。预览、应用、导出共用同一三方合并结果；导出不应用正文、不创建历史版本。
- 应用和导出绑定打开预览时的草稿 revision，草稿变化时要求重新预览。再次应用保留独立手改，不能绕过过期预览检查。

### 续聊与范围

- `@整份简历` 原本就是操作范围，并非新建对话；普通连续聊天、刷新恢复的上下文链本来有效。
- 实际断链场景是取消临时范围后前端遗漏 task_id，后端将其当成新任务。现在服务端按 owner/project/conversation 和最新任务范围恢复身份，不按用户关键词猜测。
- 从局部范围点击整份建议的“继续调整”时，旧局部范围可能残留。现在任务身份和作用范围一起恢复。
- 被动刷新不再覆盖用户刚选的范围；聊天新增轻量续聊提示。用户阅读旧消息时保持滚动位置，不因自动保存而强制滚到底部。
- 未应用时基于上一版建议继续；应用后基于最新正文继续，并保留有效对话。新对话仍隔离并清理旧聊天材料。

### 超时

- 对应失败记录在 2026-09-10 00:29:24Z 提交、00:30:05Z 失败，诊断为首次响应超时，符合原 40 秒阈值；后续同一路由请求成功。不能据此认定模型输出 Schema 出错。
- 全局 complex/vision 首次响应等待显式设为 90 秒；局部、空闲间隔与总时限不变。增加首次响应、流空闲、总时限及主动取消的独立诊断。
- 失败保留原要求和附件，重试不重复插入用户消息。不增加自动重发，不保证上游永不超时。

## 验证范围

- 真实 Chromium：范围选择/取消、解释后继续、未应用建议续改、应用后续聊、刷新、新对话隔离、历史阅读位置及未发送输入保留。
- 真实 Chromium：差异弹框、纯样式与混合变化、首次/再次应用、过期预览、PDF/Word 实际下载和文件内容检查；检查导出不改变草稿及版本。
- 隔离数据库及注入模型：超时分阶段、取消、按能力配置、失败后原文与原图保留、用户消息不重复、归属及并发检查。
- 原有 14 种布局的 1008 次真实浏览器操作、公开文件视图增删、撤销重做等保留在全量回归中。
- 本轮未新增真实 Astra 调用；注入模型的功能测试不代表模型准确率或真实延迟测试。已有模型对照结果见 `AI_COMPARISON_2026-09-09.md`。

## 收尾记录

- 首次全量收尾发现两项 UI 测试失败：旧文案断言仍限定“未应用建议失效”，与新对话删除全部旧建议的现行行为不符；该测试提前中断又保留了上下文，导致后续测试错误假定全局范围一定为空。
- 更新删除范围断言，新增新对话任务/范围/continuation 清空检查；局部改写明确断言不改变进入前的全局范围与任务。专项 7/7 通过，没有通过清空全局任务来迁就测试。
- 最终全量回归：390 项，384 通过、0 失败、6 项显式开启的真实模型测试跳过，耗时约 312 秒。`git diff --check` 通过。
- PM2 `resume` 已重启，重启前无正在生成的任务，重启后 HTTP 200。项目、资料、岗位、文件、导入、草稿、历史版本等九张受保护数据表重启前后摘要一致；未清空生产用户数据。
- 重启后真实 Chromium 只读检查实际项目：工作区返回 continuation，页面提示“延续当前对话”；当前建议预览渲染 119 个节点，“再次应用”和导出入口可见，PDF/Word 选项可展开，无脚本异常。该检查前后十张表（包括操作事件）的摘要一致，没有点击应用或修改生产正文。
- 另外检查一条真实纯样式建议：接口返回 7 项展示字段变化，差异弹框实际显示 7 条删除行、7 条新增行和“结构与排版”，不是空白差异。检查前后草稿、版本和操作事件均不变。

日志：

```text
/tmp/resume-continuity-confirmed-20260910.log
/tmp/resume-final-ui-20260910.log
/tmp/resume-ai-ui-final-20260910.log
/tmp/resume-release-verified-20260910.log
```

## 尚未实施

“生成过程中继续发送/中途追加指令”仅做可行性讨论，未开放输入队列、并行生成或自动取消重启。Astra 原地追加接口尚未确认，不能汇报为已支持。
