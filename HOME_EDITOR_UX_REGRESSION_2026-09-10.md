# 首页材料与编辑区体验回归

日期：2026-09-10。真实入口 `index.html`，沿用当前分支和已有模块，未更新历史原型、未清空用户数据。

## 本轮改动

- 已就绪文字后显示绿色呼吸点，遵循减少动态效果设置。
- 截图粘贴先展示本地缩略图；字节上传有进度，上传/识别期间可删除或替换。首页图片通过安全校验后直接作为视觉材料，生成时发送原图，不再等待 OCR 排队。Word/PDF 仍一次识别。
- 文件名与图片均可打开原图弹框，支持适应窗口/原始尺寸、错误重试、键盘关闭与焦点恢复。删除材料不会被迟到请求恢复。
- 岗位链接失败显示卡片级红色错误和输入状态；三项材料全部就绪才能生成。生成按钮与“应用修改”同色，未就绪浅灰。
- 系统样式保留 20 款；其中 15 款从对应固定上游 PDF 以 200 DPI 渲染更清晰参考图，并加内容指纹避免浏览器复用旧模糊缓存。其余保留原始来源分辨率，不把放大插值说成细节恢复。
- 首页生成结果改为居中弹框，关闭后可重新打开；PDF、Word、继续编辑直接可见。详情整份预览亦直接展示 PDF/Word 下载。
- 首次进入详情、刷新和续聊根据已受理消息冻结的材料显示截图，不依赖后来变更的首页选择，不额外复制模型输入。
- 点击正文独立图片可上传更换，保留节点位置、尺寸及展示样式，复用草稿事务、自动保存与五步撤销/重做。整页扫描背景不可当独立头像替换；没有默认 AI 裁剪。
- 编辑器工具栏顶部留白桌面约 12px、手机约 10px；保留真实纸张尺寸、缩放、吸顶及移动端抽屉；修复手机顶部偏移重复叠加。最初尝试 390–480px 聊天宽度，用户随后确认恢复原先 390px，已按新要求恢复，剩余空间归画布且不拉伸纸张。

## 针对性验证

- 首页截图、即时预览/删除、错误色、按钮、冻结资源生命周期与根路径、`/resume`、`/tools/resume` 实际反向代理图片：7/7。
- 编辑器 1440/1280/900/390 四档几何布局、首次聊天图片、建议预览/直接下载：5/5。
- 图片更换真实文件选择、取消、待保存文字、刷新、撤销/重做、权限、并发 revision、幂等、原生导出与材料流程组合：22/22。
- 首页生成完成及生成中刷新后的结果弹框、重新提供材料、图库选择焦点及大图预览另有真实 Chromium 功能覆盖。

本轮界面/事务测试使用隔离数据库及虚构材料；不声称这些测试证明 Astra 对所有真实简历的生成质量。

## 统一回归中发现并修正的测试问题

首次全量 449 项中 439 通过、3 失败、7 跳过。未直接放行：

- 浏览器 CDP 对未引用的异步求值结果可能返回 `Promise was collected`。驱动保留该次 Promise 到结算，不重试可能产生修改的表达式，并保留原始失败上下文。
- 差异弹框入场动画未完成就点击关闭，鼠标落到了正文。测试等待实际动画状态并确认关闭，仍走真实坐标点击，不使用固定睡眠或强制 DOM 点击。
- 150% 缩放的宽表格在收紧后的画布中可横向滚动；旧测试居中右侧单元格后仍在屏幕外查找父容器左边缘。测试先滚动至可见边缘再悬停，保留全部 24 条连续鼠标路径、途中暂停/刷新与增删/撤销检查。对应两项真实浏览器测试修正后通过。

## 最终发布检查

- `node --test --test-concurrency=2 tests/*.test.js`：449 项，442 通过、0 失败、7 跳过；日志 `/tmp/resume-home-editor-release-20260910.log`。跳过项包含需显式启用的真实模型调用，不计为模型质量通过。
- 确认无进行中 AI 任务后，已重启 PM2 `resume` 服务。
- 实际 HTTPS `/resume/` 部署真实 Chromium 检查通过：工具栏顶部间距 12px；现有头像、整份预览可读；点击独立图片打开更换弹框并可取消；预览直接下载可见；20 张图库缩略图加载完成，选中参考大图宽 1656px。
- 错误根路径图片请求 0，浏览器脚本错误 0；检查前后 9 张核心业务表哈希一致，未写入测试简历或改动现有正文。日志 `/tmp/resume-home-editor-live-20260910.log`。

## 后续小范围修正

- 已选系统样式缩略图改用独立只读图片查看器，只展示当前模板，不再复用图库选择流程；“系统样式”入口仍展示完整图库。
- 全局 AI 恢复固定 390px；四档屏宽、聊天收缩和吸顶不变。
- 针对性真实浏览器与图库资源/部署前缀回归 6/6，通过刷新、键盘打开/关闭当前样式、焦点恢复、选择不变与图库入口区分。日志 `/tmp/resume-selected-template-width-20260910.log`。未因小改动重复运行整个模型测试集。
