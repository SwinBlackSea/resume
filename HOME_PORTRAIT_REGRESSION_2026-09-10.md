# 首页与简历图片集成验证

日期：2026-09-10。分支：`feature/resume-first-flow`。

## 已验证的基础阶段（网络模板图库追加之前）

- 第二轮全量：432 项，425 通过、0 失败、7 项显式 opt-in 真实模型测试跳过。
- 命令：`node --test --test-concurrency=2 tests/*.test.js`。
- 日志：`/tmp/resume-home-portrait-regression2-20260910.log`。
- 第一轮发现的离线 Harness 数据库依赖、首页刷新竞态和预览弹框动画点击竞态已修复并回归；旧首页断言迁移到三材料流程，保留原编辑、复制、保存失败、一次识别与导航断言。

## 真实浏览器 / 文件验证

- 首页三卡、Word/PDF/图片输入、拖拽、岗位链接失败/成功状态、三材料服务端门槛、角色隔离、加载、刷新恢复、停止、追问答复、预览、下载、继续编辑及独立重生成。
- 1440 / 900 / 390 / 320 像素视口无横向溢出，按钮焦点可见，岗位输入无刺眼外框；删除“准备三份材料”类空闲小字，保留必要状态。
- 私有图片在真实浏览器可显示；草稿与历史版本 PDF/Word 导出保留图片，重复导出复用结果，版本保护图片不被聊天清理删除。
- 实际 Chromium PDF 经 `pdftotext` 验证可复制中文正文，并栅格化检查头像像素；短简历不额外产生空白页。
- Word 使用真实 OOXML 图片、关系与媒体字节；检查文字可编辑、纵横合并表格、换行及照片比例。
- 原生 DOC/DOCX/PDF 与扫描 PDF 图片候选、EXIF、裁剪、私有权限、取消和资源生命周期专项通过。
- 含图 Word 直接导入使用已有完整页面场景保留照片与排版，文字仍可编辑；不是宣称背景照片已经变成独立可编辑头像。后续 AI 可复用原生照片候选；单独提取失败时保留已有页面原图，无二次 OCR。

## 模型实测与模拟边界

- 真实部署 Astra：两种不同头像位置的合成简历截图，优化后每例 1 次调用、0 次恢复，裁剪 IoU 1.0000。日志 `/tmp/resume-portrait-live-optimized-20260910.log`。
- 真实部署网关：虚构软件工程师岗位页面文本完成一次语义岗位判定。
- 首页完整流程测试中的识别与生成输出主要注入可控测试模型；浏览器、上传、API、队列、渲染与文件下载是真的，但不能据此声称真实网站读取或任意模型生成的准确率达到 100%。
- 合成截图是小样本，不代表任意低清、遮挡、多人照或复杂页面都能精准识别。

## 运行边界

- PDF 完整文档导出需 Chromium 和中文字体；当前主机启用 Chromium 内核 sandbox 的专项启动未通过。当前链路保留 CSP、外部网络阻断、无文档脚本、资源权限与体积/并发/超时限制；它们不能替代进程沙箱。公开多租户部署应单独配置可用的渲染隔离环境。
- Word 与浏览器使用不同排版引擎，复杂 CSS 不能仅凭基础测试承诺像素级一致。
- 本阶段未提交或 push；未更新历史原型；存量业务数据不清空。
- 用户追加的 20 款网络模板图库有独立来源许可清单与验证记录，见 `TEMPLATE_GALLERY.md`，不混入上述基础阶段的测试结论。

## 图库集成后的最终验收与重启

- 全量回归：434 项，427 通过、0 失败、7 项需显式开启的真实模型测试跳过，耗时约 220 秒。日志 `/tmp/resume-home-gallery-final-20260910.log`。
- 图库、首页和品牌导航专项 9/9 通过；真实 Chromium 逐一预览、应用 20 款，核对服务端材料 ID、第三卡片缩略图与名称。40 张原图/缩略图实际 HTTP 字节校验、取消不改变选择、大图失败禁止应用、键盘焦点与移动端通过。
- 图库模型输入使用隔离测试模型捕获请求：确认所选原图的哈希、视觉字节及只作版式参考标记，没有冒充 20 款真实 Astra 生成质量测试。
- 图片导出追加回归 6/6 通过，含重复历史版本导出复用；日志 `/tmp/resume-image-final-20260910.log`。
- PM2 `resume` 已重启。部署后真实 Chromium 检查编辑器、整份预览、首页三材料门槛、20 款图库与大图取消、1440/900/390/320 视口、resume 双向跳转，无页面 JS 错误。
- 重启前检查没有运行中的 AI 任务；重启及只读浏览器验证前后，对项目、资料、经历、岗位、岗位附件、上传、导入、当前草稿、历史版本等 9 张业务表做内容哈希核对，均未改变。部署日志 `/tmp/resume-home-gallery-release-20260910.log`。
- 未提交或 push；上述运行安全与真实模型样本边界仍然适用。
