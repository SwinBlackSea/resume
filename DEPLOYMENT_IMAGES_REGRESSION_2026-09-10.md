# 子目录部署图片加载修复

日期：2026-09-10，分支 `feature/resume-first-flow`。

## 根因

线上页面挂载在 `/resume/`。新图库和私有头像直接使用后端返回的规范路径 `/api/v1/...`，未经过部署地址转换，图片请求绕过简历网关，落到其他服务并返回 502。此前浏览器回归只访问服务根目录，未暴露此问题。

## 修复

- 复用真实入口唯一 `toApiUrl`，精确匹配 `/api/v1` 路径段，保留查询和片段；外链、data/blob、已带前缀地址不改写。
- 文档 Renderer 注入地址解析函数，在设置 src/href 之前转换，不先发送错误请求再补救。覆盖编辑器、整份预览、历史、导入和首页生成结果。
- 差异审阅图片、聊天图片回退地址、图库缩略图/重试/大图/选中卡片使用同一转换。
- 保留规范资源 ID 和原始文档地址，不修改数据库正文、历史版本、模型协议或导出地址。

## 验证与部署

- 新增 `tests/deployment-images-browser.test.js`：真实 Chromium 配合隔离反向代理，分别在 `/`、`/resume/`、`/tools/resume/` 检查头像、预览、历史展示、差异、20 张缩略图、大图、选择与刷新；所有越过部署前缀的请求直接失败并记录。
- 专项 16/16 通过；日志 `/tmp/resume-deployment-images-20260910.log`。
- 整项目 437 项：430 通过、0 失败、7 项显式 opt-in 真实模型测试跳过；日志 `/tmp/resume-deployment-images-full-20260910.log`。
- 确认无运行中 AI 任务后重启 PM2 `resume`。真实浏览器访问实际 HTTPS `/resume/` 网关，已有简历头像、整份预览、20 张缩略图及大图全部加载成功；错误根路径图片请求 0，页面 JS 错误 0。
- 部署验收仅预览和取消，不应用、不生成；前后 9 张业务表内容哈希一致。日志 `/tmp/resume-deployment-images-live-20260910.log`。
- 没有调用大模型，不需要重新生成原简历；未提交或 push。
