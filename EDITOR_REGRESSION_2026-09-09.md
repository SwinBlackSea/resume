# 编辑体验与局部 Astra 验证 · 2026-09-09

本轮在 `feature/resume-first-flow` 继续实现，不覆盖已有成果，不维护历史原型。

## 已完成

- 删除重复的“我的简历”按钮，resume 标识返回首页；个人中心仍在顶栏最右侧，收纳制作另一份、历史版本与设置。
- 文档工具栏首位为磁盘保存图标，位于撤销旁，直接保存历史版本；预览/下载在同一工具栏。编辑页不再重复提供“生成简历”，首页生成保留。
- “显示内容增删按钮”默认关闭，保留浏览器内显式选择；关闭不影响直接改字、AI、自动保存或撤销。
- 开启后无需点击，悬停即显示纸张内的一对 `+/-` 和一个范围框。文字选择编辑节点，父容器边缘选择完整子树；复制和删除严格针对框内对象，不按按钮坐标重猜、不自动升级父层级。保留完整任意深度子树、布局位置调整及统一撤销事务。
- 聊天卡片保留摘要与操作，差异放入居中弹框。按完整行展示：新增绿底，删除红底及删除线；不逐字拆分，不叠放两份全文。未修改行可折叠，修改不截断。
- 整份预览、差异弹框与首次/再次应用都使用 A/B 对最新 C 的同一合并结果 D。查看后草稿发生变化时阻止应用并要求重新预览；聊天刷新不丢预览校验。超过五步后仍可再次应用并保留独立手改。
- 局部也使用已配置的 `gpt-6-astra`；保留纯文本 working-set、原始用户指令、真实对话顺序及严格输出协议。

## 模型兼容问题与实测

直接使用局部原来的 `thinking:false → reasoning.effort:none` 时，第三方网关实际返回 HTTP 400、`unsupported_value`，明确指向 `reasoning.effort`，最低支持 low。

通过 adapter 的显式 `RESUME_OPENAI_MIN_REASONING_EFFORT=low` 兼容，不改提示词、不改用户要求、不增加关键词路由；显式更高推理强度不降级。该名字是用户指定的网关型号，不据此断言第三方模型真实身份。

真实 Chromium 经页面、HTTP server、harness、gateway 完成三轮局部改写，全部使用虚构材料和隔离数据库：

| 轮次 | 要求 | 建议字数 | 模型耗时 |
|---|---|---:|---:|
| 原文 | 产品规划与客户服务经历 | 79 | — |
| 1 | 精简，保留 120 家客户 | 21 | 5.529 秒 |
| 2 | 继续精简，保留数字 | 13 | 5.056 秒 |
| 3 | 更专业的一句话，不增加新事实 | 16 | 5.502 秒 |

三轮均首次完成，没有协议恢复；数字保留、建议逐轮变化。应用前正文不变，实际应用和撤销检查通过。使用最终 `.env` 再独立调用一次局部接口也成功，约 5.367 秒。

这四次实际调用不代表长期稳定率或延迟保证。全局链接＋不同结构参考图的既有对照见 `AI_COMPARISON_2026-09-09.md`，本轮未重新统计那组测试。

## 回归结果

- 全量 `npm test`：380 项，374 通过、6 项条件跳过、0 失败。跳过项是显式开启才运行的真实模型测试；其中新增局部 Astra 测试已单独开启并通过。
- 全量包含 14 种布局、1008 次真实浏览器修改/撤销/重做操作；本轮早段另包含生产文档只读副本，共 15 种布局、1080 次操作通过。
- 公开 Word/PDF 独立复跑：Harvard 两份 Word、Harvard PDF、MIT 14 页 PDF（含固定布局视图），6 个视图、99 次操作通过。使用固定校验和的已有下载文件，不把浏览器结构测试当作模型识别成功。
- 局部 Astra 实测与 adapter 定向回归：10/10 通过。
- 1440/760/390px 检查个人中心最右侧；默认关闭、开关即时生效及刷新记忆、悬停无点击、单框、纸内按钮、直接编辑与撤销均通过。
- 差异弹框、整行增删、XSS 安全、空值/中文/Emoji/大文本、过期预览、首次及再次应用、超过五步后恢复、保存历史不清空撤销均覆盖。

日志：

```text
/tmp/resume-release-complete-20260909.log
/tmp/resume-public-final-dialog.log
/tmp/resume-local-astra-final.log
/tmp/resume-exact-public-final.log
```

## 部署与数据检查

- 显式激活局部与全局 Astra，旧 `.env` 保留私有备份；PM2 已重启，HTTP 200。
- 实际启动路由：text/complex/vision 均为 `openai:gpt-6-astra`；最低推理 low，严格 Schema、`store:false` 保留。
- 重启前后草稿、版本、项目、资料、岗位、文件等九张受保护表的摘要一致。操作事件表摘要有变化：既有启动整理会重新规范已归档操作摘要，不能将这次检查汇报为十张表逐字节均不变；本轮未新增业务正文清理逻辑。
- 重启后的生产页面只读浏览器复查：个人中心距右侧 20/27/27px；保存紧邻撤销、预览在工具栏、差异弹框存在、默认关闭，开启后悬停单框且按钮在纸内，无页面脚本异常。该复查前后十张受保护表摘要全部一致，没有应用或修改生产简历。
- 未提交或推送本轮改动。

## 后续：点击空白退出与撤销核查

- 空白点击此前仅隐藏快捷工具条，遗留 `.selected`、编辑焦点及锚点；真实浏览器复现残留选中框。现统一清除临时编辑选择、文字选区、焦点、工具条与悬停坐标，失焦仍走原自动保存链，不丢弃 AI 建议或任务。
- 用户报告的撤销记录：撤销后的数据库完整文档严格等于该次修改的 `before`，也等于上一笔 AI 应用的 `after`。两次连续 AI 应用之间包含根内边距、标题颜色/字号/背景等整份样式变化；撤销一次回到上一笔应用状态，而不是两次应用之前。
- 这条真实记录只读复制到隔离数据库，通过浏览器执行撤销/重做，对 112 个节点的尺寸、位置、字体、颜色、边框、背景、间距及布局样式逐项比较，均与对应修改前/后状态一致。本次未复现撤销存储或渲染损坏，未为此改写生产正文或撤销记录。
- 定向回归 38/38 通过，含纸内/纸外空白、保存/撤销、重新悬停、局部 AI、全局聊天、预览再次应用及公开文件 6 个视图的 99 次增删操作。日志：`/tmp/resume-dismiss-undo-release.log`。
- 服务已再次重启，HTTP 200。重启后在实际项目中只点击文字再点击空白：选中节点为 0、快捷条/范围框均隐藏、编辑焦点退出，无页面异常；该浏览器复查前后草稿、版本及操作记录摘要一致。

## 后续：移向增删按钮时误选父节点

- 真实浏览器连续移动复现：日期子节点移向原始加号位置，第 19/24 步误选经历父节点。原有直接移到按钮的测试没有覆盖中间路径。
- 统一悬停命中处理，在当前指针锚点与按钮之间保留窄几何通道；经过父节点或相邻内容时不切换对象。重新渲染后按稳定节点 ID 恢复该路径，离开通道仍可正常选择其他节点；没有新增拦截点击的透明覆盖层。
- 新增连续路径回归：三档缩放、四种节点、增加/删除共 24 条路径，逐步检查唯一边框与锁定对象，覆盖途中停留、刷新、点击原按钮位置、完整撤销、直接切换相邻节点及移出窗口隐藏。
- 定向回归 43/43 通过，包含 14 种布局的 1008 次真实浏览器操作、公开 Word/PDF 六个视图的 99 次操作。最终退出窗口处理调整后再跑三项浏览器回归，3/3 通过。
- 生产页面只读复查截图对应日期节点：100%、125%、150% 下共 96 个连续鼠标位置均未跳到父节点；无浏览器脚本异常，草稿、历史版本及操作记录未变。
- 日志：`/tmp/resume-hover-path-before.log`、`/tmp/resume-hover-path-regression.log`、`/tmp/resume-hover-path-final.log`。
