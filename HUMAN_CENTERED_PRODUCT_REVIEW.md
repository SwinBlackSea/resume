# 当前简历产品的人性化体验审视与改进建议

> 审视日期：2026-09-04
> 审视范围：当前真实入口 `index.html`、主要前后端交互、现有产品说明及深度测试结果
> 审视视角：第一次使用简历工具、不了解 AI 能做什么、担心内容失真和误操作的普通求职者

## 一、结论先行

当前产品最正确的决定，是把工作区稳定地收敛为“左侧资料、中间当前简历、右侧 AI 对话”三类对象，并坚持：

- 当前简历始终是一份完整文档，而不是模板和槽位的拼装。
- 用户可以直接修正已有文字，结构变化交给 AI 建议。
- AI 必须先给出建议，用户点击“应用修改”后才改变简历。
- 资料和当前简历互相独立，不静默同步。

这些原则符合新手的自然理解，不建议推翻。

目前真正影响产品品质的，不是功能数量不足，而是用户在几个关键时刻缺乏确定感：

1. 第一次进入后，不清楚“我现在最应该做什么”。
2. “草稿自动保存”和“保存为版本”的状态表达容易让人误判。
3. AI 能改哪里、会改几处、是否已经改动，反馈还不够直观。
4. AI 等待较久或失败时，缺少取消、重试和继续工作的稳定出口。
5. 导入、生成、应用修改等高影响操作，保护和撤销能力不足。
6. 个别界面能力看起来已经可用，实际上没有完整实现，会损害信任。
7. 字号、点击区域、弹窗键盘操作和响应式布局离“所有人都能轻松使用”仍有距离。

理想体验可以浓缩成一句话：

> 我把现有简历或求职目标告诉你，你一步一步帮我完善；每一步我都知道会改什么、为什么这样改，而且随时能撤回。

## 二、优先级定义

| 优先级 | 含义 |
|---|---|
| P0 | 会造成事实错误、数据覆盖、无法撤销、虚假能力感知或核心流程不可用，应立即修复 |
| P1 | 明显增加新手理解成本、操作成本或失败后的无助感，应进入最近迭代 |
| P2 | 不阻断主流程，但会降低精致度、可访问性和长期满意度 |

## 三、逐项问题与建议

### A. 第一次使用与整体方向感

#### UX-01｜首次引导把“填写资料”表现成必经步骤

- 优先级：P1
- 当前表现：首次引导依次强调管理真实资料、告诉 AI、确认修改，但产品实际上允许用户完全通过当前简历和对话完成工作。
- 用户感受：没有完整资料的新手可能认为“不先填完资料就不能开始”，从而产生负担或直接离开。
- 建议：把首次引导改成结果导向的三条可选起点：
  1. “我已有简历”——导入并继续修改。
  2. “我想边聊边写”——直接进入 AI 对话。
  3. “我想长期维护资料”——再进入个人资料。
- 验收标准：用户关闭引导后，页面上仍保留一个明确的首要行动；没有填写资料也能自然开始。

#### UX-02｜没有真正面向空白用户的起步页

- 优先级：P0
- 当前表现：页面默认依赖已有项目和当前简历；没有可用项目时主要表现为错误提示，而不是创建入口。
- 用户感受：第一次使用或删除全部项目后会遇到“没有可用的简历项目”，却不知道如何恢复。
- 建议：为空状态提供三个清晰入口：“导入已有简历”“和 AI 从零开始”“创建空白简历”。三种入口最终都进入同一个当前简历，不引入模板概念。
- 验收标准：全新账号无需后台预置数据，即可在一个页面动作内开始创建当前简历。

#### UX-03｜页面缺少持续可见的“下一步”

- 优先级：P1
- 当前表现：三个区域信息完整，但用户需要自己判断先看资料、改正文、选岗位还是和 AI 聊。
- 用户感受：功能都看得到，却不知道先后顺序，尤其容易反复打开弹窗探索。
- 建议：在中间简历顶部加入一条轻量、可关闭的下一步建议，例如“已导入简历，接下来可以补充目标岗位”或“岗位已就绪，可以让 AI 检查匹配度”。只推荐一个动作，不增加仪表盘。
- 验收标准：系统能根据当前状态给出唯一、可执行的下一步，用户也可以忽略它继续自由操作。

#### UX-04｜“生成简历”与已经存在的“当前简历”语义冲突

- 优先级：P1
- 当前表现：用户已经在编辑一份简历，顶部主按钮仍叫“生成简历”。
- 用户感受：不清楚点击后是覆盖当前简历、另存一份、下载文件，还是重新开始。
- 建议：优先改为“生成投递版”，并在按钮附近或首次悬停时说明“按当前简历和岗位生成一个可下载版本，同时保留历史记录”。
- 验收标准：未点击按钮的用户能够准确说出它的结果和是否会覆盖当前草稿。

#### UX-05｜生成前是否准备充分，只在失败后才告诉用户

- 优先级：P1
- 当前表现：岗位或关键信息不足时，用户点击生成后才可能收到“还需要……”的提示；已有的岗位覆盖信息隐藏在弹窗内部。
- 用户感受：按钮看起来可用，执行后却被退回，像是系统临时增加要求。
- 建议：在主按钮附近提供简单状态：“可以生成”或“还差目标岗位”；点击不足状态时直接展开缺失项，并允许用户让 AI 帮忙补问。
- 验收标准：所有可预先判断的阻塞项都在生成前展示，不通过启动任务来发现。

### B. 保存、版本和资料心智模型

#### UX-06｜没有待成版修改时显示“已保存为版本”，容易形成错误承诺

- 优先级：P0
- 当前表现：界面根据是否有未成版修改，将顶部状态切换为“草稿已自动保存”或“已保存为版本”；后者不一定代表用户刚刚创建过版本。
- 用户感受：用户可能误以为当前内容已有不可变备份，实际却未必存在对应的新版本。
- 建议：把两个状态彻底拆开：
  - 自动保存状态：“正在保存 / 草稿已保存 / 保存失败”。
  - 版本状态：“有更新，可保存为版本 / 当前内容已保存为版本”。
- 验收标准：任何时候都不会仅凭“没有待成版修改”推导并显示“已保存为版本”。

#### UX-07｜“自动保存草稿”和“保存为版本”的价值区别不够直白

- 优先级：P1
- 当前表现：两个保存概念同时出现，但主要依赖用户自行理解。
- 用户感受：新手不知道为什么自动保存后还要保存版本，也不知道什么时候值得保存。
- 建议：用日常语言解释一次即可：“草稿会随时自动保存；满意时保存一个版本，之后可以回来查看或继续修改。”版本按钮在有更新时显示“保存一个可回看的版本”提示。
- 验收标准：首次接触的用户能说清草稿和版本的差别，不需要理解“事务、快照、冻结”等技术概念。

#### UX-08｜资料和当前简历的独立关系只在局部提示，容易误解

- 优先级：P1
- 当前表现：产品逻辑上已严格分离，但主要通过弹窗说明和短暂提示传达。
- 用户感受：修改个人资料后可能等待当前简历自动变化；在对话中上传内容后也可能误以为已永久保存到资料。
- 建议：在两个区域各保留一句长期可见的短说明：
  - 资料：“用于以后复用，不会自动改当前简历。”
  - 当前简历：“这是本次要投递的内容，修改会自动保存。”
- 验收标准：资料变化后，用户不会预期简历被静默修改；对话内容也不会被误认成已保存资料。

#### UX-09｜个人信息卡片硬编码“产品经验”

- 优先级：P0
- 当前表现：只要存在工作年限，卡片就显示“N 年产品经验”，即使用户的职业是辅导员、教师、工程师或其他岗位。
- 用户感受：系统没有真正理解自己，甚至可能让用户怀疑后续 AI 内容也会套用错误职业。
- 建议：未确认职业类别时统一显示“N 年工作经验”；确认当前职业后才显示“N 年某领域经验”，且用户可直接修正。
- 验收标准：任何非产品岗位的测试资料都不会出现“产品经验”字样。

#### UX-10｜岗位附件统一被称为“岗位截图”

- 优先级：P2
- 当前表现：岗位卡片按文件数量显示“N 张岗位截图”，但实际可能是 PDF、Word 或其他文件。
- 用户感受：系统描述与自己的操作不一致，降低精致感。
- 建议：统一为“N 个岗位文件”；只有明确识别为图片时才使用“截图”。
- 验收标准：混合格式上传后的数量和称谓准确。

#### UX-11｜“保存为版本”禁用时没有解释

- 优先级：P2
- 当前表现：无待保存更新时按钮不可点击，但用户不知道原因。
- 用户感受：可能认为功能坏了或自己权限不足。
- 建议：禁用状态显示解释：“当前内容已经保存过；有新修改后可再次保存版本。”
- 验收标准：鼠标、键盘聚焦或轻触禁用区域时都能获得原因说明。

### C. AI 选择、提问和修改建议

#### UX-12｜作用范围表达偏技术化，长内容还会被截断

- 优先级：P1
- 当前表现：使用 `@资料 · 具体对象`、`@简历 · 具体内容`、`@整份简历` 显示范围，具体正文名称可能只截取很短字符。
- 用户感受：相似段落难以区分；`@` 对不熟悉协作工具的人也不够自然。
- 建议：保留现有三类结构，同时增加日常语言引导，例如“这次会处理：@简历 · 职业概况第一段”。对相似内容补充所在区块和序号，不仅依赖正文截断。
- 验收标准：简历中存在多条相似经历时，用户仍能明确知道 AI 将处理哪一条。

#### UX-13｜点击正文同时触发“直接编辑”和“让 AI 帮写”，两种意图互相竞争

- 优先级：P1
- 当前表现：点击可编辑文字既进入输入状态，又出现 AI 浮动工具。
- 用户感受：想改一个错别字时被额外工具打扰；想选中给 AI 时又可能误改正文。
- 建议：单击继续专注轻量编辑；选中文字后才显示 AI 工具。若未选中文字，可在段落边缘提供低干扰的“让 AI 帮写”入口。
- 验收标准：改错别字不出现遮挡；选择一段文字后，AI 操作在原位置附近清楚出现。

#### UX-14｜正文中按回车被阻止，规则只在操作后才解释

- 优先级：P1
- 当前表现：用户在正文按回车时无法新增段落，随后才被提示结构调整要交给 AI。
- 用户感受：行为像编辑器故障，而不是有意设计。
- 建议：正文进入编辑时显示简短提示“可修改文字；新增段落请让 AI 帮忙”。用户按回车后，不只弹提示，还直接提供动作“让 AI 在这里新增一段”。
- 验收标准：用户第一次尝试换行时能够立即转入正确路径，不需要自己重新描述上下文。

#### UX-15｜快捷提问只是填入输入框，但看起来像会立即执行

- 优先级：P2
- 当前表现：“更简洁”“突出工作成果”等按钮点击后只填充文字，不发送。
- 用户感受：可能反复点击，或认为功能没有响应。
- 建议：在快捷项上使用“试试这样问”的视觉表达，并在填入后自动聚焦输入框；也可将按钮文字改为完整请求，使其明显是可继续修改的草稿。
- 验收标准：首次使用者能预期点击后是“填入待发送内容”，不是直接改简历。

#### UX-16｜AI 修改卡片在窄栏里信息过密

- 优先级：P1
- 当前表现：修改前、修改后、说明和多个按钮都挤在右侧窄栏；多区块修改时阅读成本更高。
- 用户感受：用户很难快速确认 AI 到底改了哪几处，只能选择盲目应用或放弃。
- 建议：先显示一行影响摘要，例如“会修改 3 处：职业概况、工作经历、技能”；每一处可展开查看前后对比，并支持点击后在中间简历定位。
- 验收标准：包含三个以上修改位置的建议，用户无需阅读全文即可说出受影响的区块数量和名称。

#### UX-17｜“继续调整”没有说明调整的是建议，而不是已应用正文

- 优先级：P2
- 当前表现：修改卡片同时提供“应用修改”“继续调整”“暂不使用”。
- 用户感受：“继续调整”可能被理解为先应用再改，或直接改正文。
- 建议：改为“告诉 AI 再改一下”，并在输入框上方显示“正在调整这份建议，当前简历尚未变化”。
- 验收标准：点击继续调整后，原建议保持可追踪，正文不发生改变。

#### UX-18｜“尚未改动简历”这一关键安全承诺不够靠近操作

- 优先级：P1
- 当前表现：产品规则要求用户应用后才修改，但主要通过引导或整体认知表达。
- 用户感受：看到完整改写结果时，可能不知道正文是否已经改变。
- 建议：每张待应用建议卡都固定显示“尚未改动简历”；应用成功后原位置改为“已应用，可撤回”。
- 验收标准：待应用、已应用、已放弃三种状态在卡片自身即可辨认。

#### UX-19｜AI 长时间等待时不能取消，也没有稳定的后台状态

- 优先级：P1
- 当前表现：等待一段时间后才显示笼统提示；真实深度测试中，部分请求耗时超过 100 秒。
- 用户感受：不知道系统是否卡住，关闭页面是否丢失，也无法撤回误发请求。
- 建议：6 秒后显示阶段性状态，15 秒后提供“取消这次请求”和“先做别的”；后台任务在对话中保留一个持续状态卡，而不是只靠短暂提示。
- 验收标准：长请求期间用户可继续浏览和编辑；取消后不会产生迟到的修改建议。

#### UX-20｜AI 失败提示没有告诉用户原文是否安全，也缺少恢复路径

- 优先级：P1
- 当前表现：结构化结果不可用时，主要提示“AI 本次没有返回可用结果，请重试”。
- 用户感受：既担心原文已被影响，又不知道应该原样重试还是换一种说法。
- 建议：错误卡明确写出“原文没有变化”，并提供两个动作：“按原要求重试”“拆成两步完成”。对合并、拆分等任务给出对应的下一步建议。
- 验收标准：失败状态不消失，用户能在原位置恢复，不必重新选择正文和重写要求。

#### UX-21｜常见的混合结构请求成功率不足

- 优先级：P0
- 当前表现：深度测试中，“合并后再扩写”“拆成三段并重组”等符合真实用户表达的请求，出现多次结构化结果校验失败。
- 用户感受：最需要 AI 的复杂任务反而不稳定，用户会退回手工操作或失去信任。
- 建议：
  1. 将复杂要求先转成可见的执行计划，例如“先合并两段，再拆成三段”。
  2. 后端在不改变用户意图的前提下自动修复格式，必要时拆为连续建议。
  3. 为职业概况、工作经历、项目经历、教育经历、技能等跨区块组合建立固定回归集。
- 验收标准：高频结构请求的端到端成功率达到既定门槛；失败时能够自动降级为可执行的分步建议。

#### UX-22｜AI 会把单个事实扩写成“多个”等未经确认的事实

- 优先级：P0
- 当前表现：深度测试中曾把一个从 0 到 1 项目写成“多个”项目。
- 用户感受：简历内容可能失真，直接影响求职诚信和面试表现。
- 建议：默认启用“只使用已有事实”的写作约束；数字、数量、范围和职责扩大一律需要明确依据。信息不足时，AI 应追问或使用不夸大的表达，不能自行补全。
- 验收标准：单个项目、单次经历和模糊数量在所有扩写测试中都不会被放大；新增事实必须先询问用户。

#### UX-23｜AI 应用后的撤回链路当前不可用，且 6 秒入口过短

- 优先级：P0
- 当前表现：后端应用 AI 修改返回 `change_event_id`，前端读取 `change_id`，导致撤回无法取得正确标识；同时撤回条仅短暂出现。
- 用户感受：用户在高影响修改后看到了“撤回”，却可能无法真正恢复，属于严重信任问题。
- 建议：先统一返回字段并补齐端到端测试；再将“撤回上一次修改”作为持续可见能力放在简历区域，至少保留到下一次修改，而不是只显示数秒。
- 验收标准：文字编辑、AI 修改和文件导入都能实际撤回；刷新页面后最近一次可撤回操作仍可恢复。

#### UX-24｜“开始新对话”过于突出，会和当前未处理建议竞争

- 优先级：P2
- 当前表现：新对话是右栏顶部常驻操作，即使当前还有未应用建议。
- 用户感受：容易把“换个问题”误解成“清空现在的一切”，或误触后丢失上下文。
- 建议：存在未处理建议时，将按钮降为次级并显示数量提醒；确认文案明确“当前简历不会变化，未处理的建议将结束”。
- 验收标准：任何会结束未处理建议的操作都提前说明影响范围。

#### UX-25｜对话中的“上传”实际上更接近资料导入，行为与位置不一致

- 优先级：P1
- 当前表现：文件从对话输入区上传后进入资料导入流程，但界面看起来像把附件只交给本次消息。
- 用户感受：不知道文件会保存在哪里、是否进入长期资料、是否已随消息发送。
- 建议：若当前能力是补充长期资料，将入口改名为“补充资料”，上传后显示文件卡和“不会自动修改简历”。如果未来支持仅供本次对话参考，应把两种行为明确分开。
- 验收标准：上传前后都能准确说明文件的保存位置和用途，不发生静默写入或含糊归属。

#### UX-26｜语音按钮展示了尚未真正具备的能力

- 优先级：P0
- 当前表现：按钮会显示“正在进行 AI 访谈”“语音已转写，AI 正在提取事实”，但当前没有完整的录音、转写和事实确认链路。
- 用户感受：用户会认为自己的声音已经被记录和处理；实际没有结果时，会直接伤害产品可信度。
- 建议：在完整实现权限申请、录音状态、转写预览、取消和确认之前，先隐藏该入口；如果保留试验入口，必须明确标注“暂未开放”，不能模拟成功状态。
- 验收标准：所有可见按钮都有真实、完整、可验证的后续结果。

### D. 人工编辑、资料管理和文件导入

#### UX-27｜资料中的经历缺少简单、直接的纠错能力

- 优先级：P1
- 当前表现：基础资料可以编辑，但工作和项目经历主要引导用户“和 AI 聊”；已有后端能力未完整呈现在用户界面。
- 用户感受：长期资料里即使只错了一个日期或公司名称，也必须通过对话纠正，过程过重且结果不够确定。
- 建议：资料允许直接新增、删除、排序和修正事实字段；仍然不在当前简历中提供人工结构工具。AI 可以帮助整理表达，但事实控制权留给用户。
- 验收标准：用户能在资料区直接修正公司、岗位、时间和项目名称，且不会自动改动当前简历。

#### UX-28｜自动保存失败后的状态不够持续

- 优先级：P1
- 当前表现：保存成功与失败主要依赖短暂提示，页面没有稳定的“仍未保存”警告；连续编辑也缺少断网保护的明确反馈。
- 用户感受：网络异常时看得到新文字，却不知道它是否已真正保存，关闭页面风险不明。
- 建议：顶部保存状态始终显示“正在保存 / 草稿已保存 / 保存失败，正在重试”；失败时保留本地待保存内容并提供手动重试，恢复后自动消除警告。
- 验收标准：断网、超时和并发冲突下，用户都能看到持续状态，未保存内容不会无提示丢失。

#### UX-29｜导入确认按钮没有表达“替换整份当前简历”的影响

- 优先级：P0
- 当前表现：导入预览的主按钮叫“确认并应用”，但操作会替换当前草稿；若当前已有未成版修改，缺少专门保护选择。
- 用户感受：容易把它理解为补充内容，点击后才发现整份简历被替换。
- 建议：
  1. 主按钮改为“替换当前简历”。
  2. 按钮上方明确说明“当前简历会被替换，原内容仍可从历史版本查看”。
  3. 检测到未保存为版本的更新时，先提供“保存当前版本后替换”或“直接替换”。
- 验收标准：导入覆盖前必须明确确认对象和结果，且始终具备可恢复路径。

#### UX-30｜上传简历和岗位信息前缺少隐私说明

- 优先级：P1
- 当前表现：简历含有高敏感个人信息，但上传入口附近没有稳定、易懂的隐私说明。
- 用户感受：会担心身份证明、联系方式、公司经历如何被存储和使用。
- 建议：在上传区直接说明“文件仅用于整理你的简历，不会公开；删除项目时可一并删除”。具体的数据保留和模型使用规则应与真实政策完全一致，避免空泛承诺。
- 验收标准：用户在选择文件前即可了解主要用途、可见范围和删除方式。

#### UX-31｜双栏导入预览正确，但长简历阅读负担较大

- 优先级：P2
- 当前表现：原文件和识别结果并列有助于核对，但固定宽度、较小文字和长页面滚动让比对困难。
- 用户感受：难以发现少量但重要的日期、数字或标题错误。
- 建议：增加“只看可能有问题的地方”摘要，优先列出姓名、联系方式、日期、数字和区块缺失；点击问题可同时定位左右两侧。
- 验收标准：用户无需逐行滚动，就能检查高风险识别结果。

### E. 生成、任务状态和历史版本

#### UX-32｜生成任务一经点击就启动，缺少最后确认、取消和直接重试

- 优先级：P1
- 当前表现：点击主按钮后立即启动生成；后端虽有取消、重试能力，界面没有对应操作。
- 用户感受：选错岗位或发现资料不完整时，只能等待；失败后也要重新开始。
- 建议：点击后先展示一页极简确认：“使用当前简历 + 当前岗位生成投递版”，允许返回修改。任务运行中提供取消，失败后在原任务卡直接重试。
- 验收标准：生成前可确认输入对象，运行中可取消，失败后可原地重试。

#### UX-33｜生成进度使用“冻结本次输入”等内部术语

- 优先级：P2
- 当前表现：进度步骤包含“校验资料与岗位”“冻结本次输入”“渲染 PDF 与 DOCX”等系统实现语言。
- 用户感受：知道系统在工作，但不知道这些步骤对自己有什么意义。
- 建议：改成用户结果语言，例如“确认这次使用的内容”“针对岗位优化表达”“生成下载文件”“检查是否有夸大或遗漏”。
- 验收标准：进度文案不要求用户理解技术实现，也不会承诺尚未完成的结果。

#### UX-34｜关闭生成弹窗后，没有稳定位置继续查看任务

- 优先级：P1
- 当前表现：文案表示任务会继续运行，但关闭后主要依赖短暂提示和历史版本刷新。
- 用户感受：不知道任务是否仍在进行、何时完成、失败后去哪里处理。
- 建议：在顶部保留一个任务状态入口，例如“正在生成投递版”；完成后变为“投递版已生成，查看”，失败后变为“生成失败，重试”。
- 验收标准：任务从启动到完成、失败或取消始终可从页面固定位置找回。

#### UX-35｜历史版本缺少更贴近求职目标的辨识信息

- 优先级：P2
- 当前表现：列表主要依赖时间、类型和通用摘要辨认。
- 用户感受：版本多以后，很难一眼分清哪个用于哪家公司、哪个最满意。
- 建议：默认使用“岗位名称 + 公司 + 日期”作为可读标题；允许用户轻量重命名，但不建立版本树或模板体系。
- 验收标准：存在十个以上版本时，用户仍能快速找到特定投递目标。

### F. 可访问性、响应式和界面精致度

#### UX-36｜多处字号和点击区域过小

- 优先级：P1
- 当前表现：部分状态、作用范围、标签使用约 7–10px 字号；部分关闭、快捷操作和工具按钮小于舒适点击尺寸。
- 用户感受：高分屏、视力较弱或触屏设备上难以阅读和准确点击。
- 建议：正文辅助文字不低于 12px，关键状态不低于 13px；主要可点击区域至少约 40×40px，视觉图标可以更小但热区不能缩小。
- 验收标准：在 100% 缩放和常见触屏设备上，所有主要操作无需精确瞄准。

#### UX-37｜弹窗缺少完整的键盘和读屏语义

- 优先级：P1
- 当前表现：弹窗缺少稳定的对话框语义、标题关联、焦点锁定和关闭后的焦点恢复；部分“×”没有清楚名称。
- 用户感受：键盘用户可能把焦点移到弹窗背后，读屏用户也难以理解当前上下文。
- 建议：补齐 `role="dialog"`、`aria-modal`、标题关联、焦点循环、Escape 只关闭最上层弹窗，以及关闭后返回触发按钮；上传区域本身也应可用键盘操作。
- 验收标准：全程只用键盘可完成导入、资料修改、应用 AI 建议和版本查看。

#### UX-38｜中间宽度存在布局溢出风险

- 优先级：P1
- 当前表现：三栏最小宽度总和高于当前响应式切换点，部分中等宽度窗口可能先发生横向拥挤或溢出，之后才切换布局。
- 用户感受：右侧按钮被挤压、正文过窄或页面出现不必要的横向滚动。
- 建议：让响应式断点与三栏实际最小宽度一致；先将右侧 AI 收为可展开面板，再在更窄尺寸下将左侧资料折叠为顶部入口。
- 验收标准：从桌面宽屏连续缩小到平板宽度时，不出现横向滚动和不可点击控件。

#### UX-39｜短暂提示消失过快，也不能被辅助技术稳定读取

- 优先级：P1
- 当前表现：多数提示约 1.8 秒后消失，错误、保存和任务状态都可能只通过提示出现。
- 用户感受：来不及读完，切换注意力后无法找回；读屏软件也未必及时播报。
- 建议：普通成功提示至少保留 3–4 秒；错误和需要行动的消息持续显示到用户处理；增加合适的 `aria-live`，同时将关键状态落在页面固定位置。
- 验收标准：所有重要失败都不是仅靠自动消失的提示传达。

#### UX-40｜账号按钮看起来可用，但没有明确结果

- 优先级：P2
- 当前表现：顶部头像式按钮有可点击外观，但当前缺少完整菜单或目的说明。
- 用户感受：点击无结果会让用户怀疑页面失灵。
- 建议：没有可交付功能前隐藏；实现后至少提供账户信息、隐私设置和退出入口。
- 验收标准：所有带按钮外观的元素均有明确、可见的操作结果。

## 四、建议的改进顺序

### 第一阶段：先恢复信任和安全感

1. 修复 AI 修改撤回字段不一致，并延长可撤回入口。
2. 修复“已保存为版本”的错误状态表达。
3. 隐藏未真正实现的语音能力。
4. 处理 AI 事实放大，默认只使用已有事实。
5. 提高合并、拆分、扩写等结构请求的稳定性和失败降级能力。
6. 为导入替换增加明确确认和当前草稿保护。
7. 修复“产品经验”等错误职业描述。

### 第二阶段：让新手始终知道下一步

1. 增加空白用户起步路径。
2. 重写首次引导，允许“导入”“直接聊”“维护资料”三种起点。
3. 在主页面提供单一、动态的下一步建议。
4. 明确资料、当前简历、草稿和版本之间的关系。
5. 让生成前的准备状态、输入对象和结果更加明确。

### 第三阶段：完善 AI 的掌控感和错误恢复

1. 修改卡先显示影响摘要，再展开逐处对比。
2. 每张建议明确“尚未改动 / 已应用 / 已放弃”。
3. 长请求支持取消、后台继续和持续状态。
4. 失败卡支持原要求重试和分步完成。
5. 澄清对话上传文件的用途。

### 第四阶段：补齐产品品质

1. 放大字号和点击热区。
2. 完善弹窗、键盘、读屏语义。
3. 调整中等屏幕响应式断点。
4. 让任务、保存和错误状态不再依赖短暂提示。
5. 改善历史版本命名和长文档核对体验。

## 五、建议持续跟踪的产品指标

这些指标用于判断产品是否真的变得更容易，而不是只判断功能有没有上线：

| 指标 | 建议观察方向 |
|---|---|
| 首次进入到第一次有效操作耗时 | 用户能否快速导入、直接提问或开始编辑 |
| 第一次 AI 建议应用率 | 建议是否易懂、可信、符合意图 |
| AI 建议应用后撤回率 | 过高说明修改不符合预期；撤回失败必须为 0 |
| 结构请求成功率 | 合并、拆分、扩写、重组及混合请求分别统计 |
| 未经确认的新事实出现率 | 目标必须为 0 |
| AI 请求等待中放弃率 | 用于判断性能反馈和取消能力是否有效 |
| 导入后立即撤回或离开率 | 用于识别替换结果是否超出预期 |
| 生成任务失败后的重试完成率 | 判断错误恢复是否真正有用 |
| 保存状态误解率 | 可通过访谈询问“现在内容保存在哪里” |
| 键盘主流程完成率 | 导入、编辑、AI 修改、保存版本均应可完成 |

## 六、明确不建议增加的内容

为了保持当前产品方向简洁，以下做法不应作为上述问题的解决方案：

- 不新增模板中心、模板版本或排版预设。
- 不把当前简历拆回“正文、模板、槽位绑定”等多个对象。
- 不增加人工区块工具栏、人工/AI 模式切换或 Word 式编辑器。
- 不强迫用户先完善全部个人资料才能开始写简历。
- 不让 AI 自动应用修改，也不让资料和简历静默互相同步。
- 不建立面向用户的“来源、证据链、派生关系”系统。
- 不用更多弹窗和教程掩盖流程问题；优先让主界面的下一步自然可见。

## 七、最终评价

当前产品已经具备一套有辨识度且正确的基础原则，但尚未把这些原则完整转化成用户可感知的安全感。下一步最值得投入的不是继续增加功能，而是把“我现在该做什么”“AI 到底会改什么”“内容是否已保存”“出错后如何回来”四件事做到毫无疑问。

当高影响操作都有清楚预告、AI 建议都有明确边界、所有修改都能可靠撤回、等待和失败都有稳定出口时，这个产品才会从“功能完整的 AI 简历工具”进入“普通人敢放心交给它处理重要求职材料的产品”。
