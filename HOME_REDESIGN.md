# 首页三材料流程

实施日期：2026-09-10。以真实 `index.html` 为入口；历史原型未维护。

## 产品边界

首页由三个主要材料区组成：个人信息、岗位信息、简历模板。每区选择一份 Word（DOC/DOCX）、PDF 或图片；卡片支持拖拽及聚焦后粘贴截图。岗位支持直接粘贴链接，模板支持二十份本地内置版式参考。三个区域全部就绪后才允许生成。

上传立即准备材料，但不自动生成简历。生成成功仍留在首页，在居中弹框显示同一完整文档的只读预览，直接提供下载 PDF、下载 Word、继续编辑；关闭后保留“查看简历”入口，不在材料卡片下面平铺正文。替换已受理材料或重新生成时创建独立项目，不覆盖原简历。取消“我的简历”列表界面，不删除项目、版本、旧 URL 或已有数据。`resume` 仍在首页和上次打开的编辑页之间跳转。

截图粘贴后先用本地临时地址即时展示，可点击缩略图或文件名查看原图；上传字节期间显示进度，并支持删除或替换。首页截图校验图片类型、大小和像素边界后作为视觉材料就绪，不排队执行 OCR；生成时按材料角色向视觉模型发送原图。Word/PDF 仍执行一次文档识别。就绪标记不等于已识别出全部事实。删除只解除本轮选择，迟到请求不得重新绑定，已被文档或有效任务引用的图片不会被物理删除。

第三卡片的已选系统样式缩略图只打开当前样式的只读大图，不跳到所有样式列表、不重复应用。只有“系统样式”按钮打开图库供更换；关闭单图预览回到原缩略图，刷新后仍遵循同一行为。

初始正文生成成功沿用现有自动草稿/不可变版本事务。编辑页现有正文的 AI 修改继续遵循“应用修改”，不因本次首页改造放宽。局部 AI、全局 AI、直接编辑、撤销和历史版本不建立另一套实现。

模型确实需要补充事实时，首页才显示原样问题和简短回答框；用户回答原样作为下一条消息，亦可进入编辑页继续沟通。不是所有生成都先经过追问。

## 组件与数据流

```text
index.html 三材料卡片
  → home-controller.js：上传、验证、生成等待与恢复、预览导航
  → /home/intakes：独立项目及材料选择身份
      → uploads：安全上传与私有文件
      → document-imports：Word/PDF 一次识别和完整文档候选
      → document-assets：截图安全校验、不可变图片和生成时视觉输入
      → job-links + home-job-validation：真实网页读取、岗位语义验证
  → /prepare：三角色就绪校验及原始请求
  → 现有 ai/messages → harness → model-gateway
  → 现有草稿与首次版本事务
  → 现有 ResumeDom.Renderer / 草稿下载 / 编辑页
```

`server/lib/home-materials.js` 负责角色、完整文档参考及就绪判定。`server/modules/home.js` 负责首页材料工作流及项目复制。`server/lib/home-job-validation.js` 只负责公开网页的岗位语义判定，结构化输出由既有网关提供。供应商、模型配置和主生成协议未另建。

模型输入的 `workspace.materials.home_intake` 包含：

- `intake_id`：本轮首页材料身份。
- `role_rules`：个人事实、岗位要求、排版参考的不同使用边界。
- `roles.personal/job/layout`：文档包含 `document_import_id`、`upload_id`；首页图片包含 `kind:image`、`upload_id`、`asset_id`；内置样式另含完整版式参考。
- 每个识别文档另外标记 `material_role`，图片候选沿用角色，不允许把岗位/模板人物当作求职者头像。

本轮材料角色冻结在已受理消息元数据中；任务内续聊读取冻结角色，而不是重新读取后来改变的首页选择。角色不建立独立个人档案、证据映射或模板绑定。

## API

| 接口 | 行为 |
| --- | --- |
| `GET /home/layouts` | 返回内置完整文档参考的轻量目录 |
| `POST /home/intakes` | 幂等创建空白项目；可用 `copy_intake_id` 复用三份已识别材料 |
| `GET /home/intakes/:id` | 当前材料状态、项目/对话身份、三项 ready、是否已尝试及已生成 |
| `PUT /home/intakes/:id/materials/:role` | 选择上传 ID（首页图片携带 `image_material:true`），或为 layout 选择 `layout_id` |
| `DELETE /home/intakes/:id/materials/:role` | 解除本次选择，不删除底层共享文件 |
| `POST /home/intakes/:id/job-link` | 真正读取网页并语义确认岗位；失败明确提示文件/截图替代 |
| `POST /home/intakes/:id/prepare` | 后端再次校验三份材料；返回现有 AI 接口请求；可承接原文 `instruction` |
| `POST /projects/:id/ai/messages` | 通过 `home_intake_id` 服务端装配、复核角色和归属，不信任客户端 ready |

所有材料 API 都校验 owner/project。样式参考不存在、文件未就绪、识别不安全、跨项目和缺材料均不能绕过后台门槛。`/prepare` 不能对已有正文自动覆盖生成。失败原请求重试复用既有任务；材料变化先复制成新的独立项目。

## 数据库与缓存

新增 `home_intakes(id, owner_id, project_id, state_json, created_at, updated_at)`，按 owner/更新时间索引。JSON 仅保存三份材料引用、链接文本及验证状态，不另存完整简历。原生文档/识别候选仍在现有 `document_imports`；当前正文仍唯一位于当前草稿。

复用材料复制识别候选记录，不重新调用识别器；同一 owner 的不可变上传和图片资源沿用引用，不复制二进制。没有模板表新增、模板版本或模板绑定。

同一组合中相同成功岗位链接五分钟内复用验证结果；失败不缓存为成功。未完结验证超过一分钟后显示可重试失败，避免进程重启留下永久等待状态。前端 sessionStorage 只保存 intake 身份、创建幂等键及未提交 URL/回答草稿，不保存完整简历副本。

## 失败与并发

- 三份材料未全部 ready、正在上传/识别/链接验证、输入链接发生未验证变化时禁用生成；后端独立重验。
- 岗位网页走既有 DNS/IP、跳转、大小和超时防护；再走结构化岗位语义判定，不用职责关键词硬匹配，支持任意语言。
- 岗位验证总时限 45 秒；新验证取消旧请求，结果用持久 token 核对，迟到结果不覆盖新选择。验证中可继续改链接或换文件。
- 新项目创建和从同一旧组合复制均带幂等保护；客户端共享创建/复制 Promise，三栏并行操作不会分散到多个项目。
- 生成中刷新通过同一 intake 查询现有 AI 运行状态，不重复发送；停止沿用运行 ID 取消及迟到结果保护。
- 已受理后失败/取消/追问仍冻结该轮材料；重新上传创建新 intake，而原要求重试留在原任务。
- 模型实际追问提供原文回答入口；网络失败保留材料、恢复状态并允许重试，不将错误当作用户回答。
- 导出绑定实际预览草稿 revision；过期则失败提示，不导出另一版内容，也不隐式创建版本。

## UI 与可访问性

宽屏三列，700px 以下纵向堆叠；测试覆盖 1440、900、390、320px。卡片有命名区域、可聚焦拖放/粘贴入口；按钮和表单有可访问名称，材料、生成、错误有状态播报。键盘可完成上传、样式选择、继续和回复；输入聚焦仅使用克制边框，按钮保留清晰焦点。生成按钮复用已有旋转标识与减少动态效果规则。

## 验证记录

- `tests/home-materials.test.js`：三栏后台门槛、归属、静态样式、首份自动版本、幂等、资料不自动入档、复制与一次识别复用、岗位语义验证与迟到隔离、失败原请求重试。
- `tests/home-redesign-browser.test.js`：真实 Chromium 拖拽/选择截图上传、失败/成功链接状态、响应式、内置样式、生成中刷新、首页预览、真实 DOCX 下载、独立重生成、继续编辑和 logo 双向导航、自然追问原文回答、取消及换材料。
- 上述六项测试通过；识别器和主生成模型使用虚构数据注入，链接测试注入公开网页返回，不声称这些是实际网站成功率测试。
- 另外单独调用实际部署网关做了一轮岗位语义验证：仅发送虚构招聘文本，成功得到“软件工程师 / 星桥测试公司”。未读取或修改业务数据库，也未输出密钥。此结果仅证明该接口形状和一轮语义判定可用，不等于头像、所有网站或全部模型准确率验证。
- 旧统一输入、列表及原型测试由主代理迁移到真实新行为；其他保留模块继续全量回归。
