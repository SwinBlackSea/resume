# ─────────────────────────────────────────────────────────────
# 简历星球 · 可选配置（复制本文件为 .env 后填写，.env 不会被提交）
#   cp .env.example .env
# 改完执行：npm start（重新启动服务后生效）
#
# 未配置任何模型时，系统使用内置规则引擎（离线可跑，不编造事实）。
# ─────────────────────────────────────────────────────────────

# ── 大模型（LLM）─────────────────────────────────────────────
# 填了下面三行即启用真实模型；调用失败会直接向用户报错，不会回退到本地引擎。
# 若完全不填，则使用内置规则引擎作为默认实现。
RESUME_LLM_PROVIDER=http
RESUME_LLM_API_KEY=sk-在这里填你的密钥
# 必须是 OpenAI 兼容的 /chat/completions 端点，举例：
#   OpenAI      https://api.openai.com/v1/chat/completions
#   DeepSeek    https://api.deepseek.com/v1/chat/completions
#   月之暗面    https://api.moonshot.cn/v1/chat/completions
#   阿里通义     https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
#   本地 Ollama http://127.0.0.1:11434/v1/chat/completions
RESUME_LLM_ENDPOINT=https://api.openai.com/v1/chat/completions
RESUME_LLM_MODEL=gpt-4o-mini

# ── 云 OCR（可选）───────────────────────────────────────────
# 不填也能用：图片岗位截图会提示你直接粘贴文字（不臆造内容）。
# 接口契约：POST {file_name, mime_type, content_base64} → {text, confidence}
RESUME_OCR_ENDPOINT=
RESUME_OCR_API_KEY=

# ── 其它（可选）─────────────────────────────────────────────
# PORT=8787
# RESUME_DB_PATH=./data/resume.db
# RESUME_FONT_PATH=/home/ubuntu/.fonts/NotoSansSC.ttf
# RESUME_DOWNLOAD_SECRET=换成一个随机字符串
