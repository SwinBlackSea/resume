# ─────────────────────────────────────────────────────────────
# 简历星球 · 可选配置（复制本文件为 .env 后填写，.env 不会被提交）
#   cp .env.example .env
# 改完执行：npm start（重新启动服务后生效）
#
# AI 对话需要配置 DeepSeek；未配置时接口会明确报错，不使用规则引擎替代语义理解。
# ─────────────────────────────────────────────────────────────

# ── 大模型（LLM）─────────────────────────────────────────────
# Resume Harness 直接调用 DeepSeek Chat Completions，并由应用管理有界会话上下文。
RESUME_LLM_PROVIDER=deepseek
RESUME_LLM_API_KEY=sk-在这里填你的密钥
RESUME_LLM_ENDPOINT=https://api.deepseek.com/chat/completions
RESUME_LLM_MODEL=deepseek-v4-flash-vision-exp
RESUME_LLM_FIRST_TOKEN_MS=30000
RESUME_LLM_IDLE_MS=30000
RESUME_LLM_TOTAL_MS=180000
RESUME_LLM_MAX_TOKENS=4096
# 全局 AI 按简历复杂度动态选择输出预算；首次请求偏向最小变化区域，
# 遇到截断或不完整 JSON 时最多提高一次，但不会突破下面的硬上限。
RESUME_LLM_MIN_OUTPUT_TOKENS=4096
# 首次请求的软上限；恢复请求可在确有需要时继续提高。
RESUME_LLM_INITIAL_MAX_TOKENS=16384
# 所有全局 AI 请求的绝对上限，防止异常输出失控。
RESUME_LLM_MAX_TOKENS_LIMIT=32768
# 文档结构识别会返回较长 JSON；未设置时沿用上面的值。
# RESUME_DOCUMENT_AI_MAX_TOKENS=10000

# ── 云 OCR（可选）───────────────────────────────────────────
# 不填也能用：图片岗位截图会提示你直接粘贴文字（不臆造内容）。
# 接口契约：POST {file_name, mime_type, content_base64} → {text, confidence}
RESUME_OCR_ENDPOINT=
RESUME_OCR_API_KEY=

# ── 其它（可选）─────────────────────────────────────────────
# PORT=8787
# RESUME_DB_PATH=./data/resume.db
# RESUME_FONT_PATH=/home/ubuntu/.fonts/NotoSansSC.ttf
# RESUME_DOWNLOAD_SECRET=换成一个随机字符串
