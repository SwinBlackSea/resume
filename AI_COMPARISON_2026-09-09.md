# 全局 AI 完整流程对照（2026-09-09）

结论：本组参考图换版场景中，`gpt-6-astra` 明显比测试时的 `deepseek-v4-flash-vision-exp` 稳定；不是对所有 DeepSeek 型号的结论。测试阶段未切换线上配置；测试完成后，用户明确授权部署，见下方记录。

## 测试后部署（2026-09-09）

- 按用户“全局使用 Astra，执行”指令，线上 `.env` 显式配置全局 `complex/vision` 为独立 OpenAI Responses 网关的 `gpt-6-astra`，包括全局协议恢复；局部 `text/deepseek-v4-flash` 与原配置保持不变。
- 旧配置已保存为私有备份，服务重启成功，HTTP 200。实际启动日志显示文字、复杂结构、视觉三个能力的有效供应商和型号。
- 使用生效配置实际调用全局文字和图片接口各一次：姓名提取、红色方形/蓝色圆形识别均通过；仅发送虚构测试内容，不使用生产简历。
- 本次定向回归 27/27 通过，覆盖配置激活、供应商隔离、严格 Schema、取消/超时、混合动作恢复及真实浏览器断线恢复。生产页面 Chromium 只读复查无脚本异常。
- 切换及重启前后十张受保护业务表内容摘要一致，未改写原简历、资料、岗位和历史版本。以下对照结果仍保留原测试条件，不改写成切换后的统计。

## 方法

- 真实 Chromium、真实 `index.html`、HTTP server、harness、供应商请求。数据库和附件隔离，使用虚构个人信息，没有发送用户的生产简历。
- 每种模型三轮，同一份原始双栏简历、单栏参考图及岗位链接；参考图使用另一人的姓名、电话、学校，检测是否混入参考人的资料。
- 岗位链接为 Anthropic Product Manager 页面；本轮由应用实际读取，不是模拟链接成功。
- 两个供应商都使用 low 推理、相同输出协议和有界恢复。轮次交替供应商顺序。继续调整承接各自上一轮实际结果。
- 每个生成结果在真实页面预览、应用、撤销及重做；最终独立重渲染，分别检查可见事实、布局、编辑边界、越界和文本块重叠。
- 原始跑分曾用编辑标记提取文字，误把不可编辑文字算为缺失。最终结论采用 `audited-report.json` 的可见文字复核，不采用该错误指标。

## 三轮结果

| 场景 | 当前 DeepSeek 视觉实验版 | gpt-6-astra |
|---|---:|---:|
| 岗位链接＋原简历图＋不同结构参考图首次生成 | 0/3 | 3/3 |
| 当前完整简历按新图换成单栏 | 0/3 | 3/3 |
| 无新附件，沿用历史图片继续精简 | 1/3 | 3/3 |
| 复制独立简历，再按图片换成双栏 | 3/3 | 3/3 |
| 合计 | **4/12** | **12/12** |

六次受限链接失败检查均通过，未计入模型准确度分母。

DeepSeek 共调用 17 次，Astra 12 次；Astra 十二项均一次完成。端到端中位耗时分别约 68.3 秒与 67.8 秒，最长约 182.1 秒与 113.4 秒。样本量小，这里的最长耗时不代表长期延迟保证。

DeepSeek 失败详情：

- 两次首次生成可见文字完整，但多个正文没有编辑边界，不能直接编辑。
- 一次首次生成在一次恢复后仍触及输出上限，没有生成完整结果。
- 三次单栏换版及两次承接精简保留旧根节点 `display:flex`，虽然模块顺序已改变，仍横向挤列。
- 其余四项通过。已生成结果的原始关键事实均保留，没有观察到参考人的姓名、电话、学校混入。

## 修复后的独立复测

通用协议 v29 明确：整份重构也继承省略样式，需要显式覆盖/清除失效布局属性；新增正文须有真实编辑边界。不改写用户指令，不使用关键词路由。

两种模型各补测一次完整换版：Astra 一次通过；DeepSeek 恢复后仍未通过，因为其声明保留原文、实际却改写内容。该补测不混入上述三轮基线统计。

## 同期修复与回归

- 原生 `section` 经历支持完整子树增删，不依赖特定 `entry` 标签或固定层数；保留显式内部段落操作。
- `+/-` 在窄页边距改为纵向排列，100%/125%/150% 检查不遮挡标题。
- 顶部导航、预览、应用按钮统一层级和尺寸，窄屏换行而非挤压。
- 当前会话已应用建议支持按需预览与再次应用；压缩保存执行材料，不依赖五步撤销，不覆盖无关手改；旧材料已删除时明确报错。
- 修复快速撤销后立刻再次应用读到旧 revision 的竞态。
- 正文提取包含非 editable 可见文字，避免后续上下文漏读。
- 修复 PDF 同字形不同 Unicode 的反查问题，保留真实原字符；PDF/Word 通过实际浏览器下载后的关键文字核验。
- 公开 Harvard Word/PDF、MIT 14 页 PDF（含固定布局投影）合计六个文档视图，真实浏览器完成 99 次结构操作回归。

最终自动化回归：348 项，343 通过、5 项条件跳过、0 失败；真实模型对照和公开文件回归单独执行。服务已重启，HTTP 200；重启前后十张受保护业务表内容摘要一致，`.env` 未变化。生产页面只读浏览器复查无脚本异常。

## 产物与限制

三轮原始记录、最终复核及截图：

```text
.runtime/ai-comparison/browser-1788921358204/report.json
.runtime/ai-comparison/browser-1788921358204/audited-report.json
.runtime/ai-comparison/browser-1788921358204/*-audited.png
```

v29 独立复测：

```text
.runtime/ai-comparison/browser-1788922600668/report.json
```

本次是有限虚构数据和一个实际岗位链接的评测，不是所有简历、样式或长期稳定性的保证。截图用于留档；没有把自动几何检查说成人工审美评分。下载检查证明文字完整性，不代表 PDF/Word 与网页像素级一致。两家 token 计量不同，不以 token 数直接推算费用。`gpt-6-astra` 是用户提供网关的模型标识，测试不能证明其底层模型身份。
